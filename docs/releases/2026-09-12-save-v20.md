# v20 存档进度迁移发布记录

- 业务提交：`22ab00f`；前端与云服务 release：`20260912_22ab00f`，上一版本均为 `20260912_5018e4d`。
- 入口：`http://223.6.255.187:5174/`、`https://223.6.255.187/`。
- 发布内容：[v19 → v20 迁移规则](../save-v20-migration.md)，按用户选定的旧累计经验换算角色、结算职业升级、卸下全部方案中不满足等级的装备。更早版本保留但不恢复可玩。
- 玩家刷新并继续游戏时迁移；本地先保留原始备份，云端复用同一校验。未批量改写玩家云档，未上传用户提供的真实存档。

## 验证

- 693 项单元测试、4 项云服务集成测试通过；调整离线结算与迁移保存顺序后，45 项相关单测再次通过。
- TypeScript / Vite 构建通过，保留既有大 chunk 提示。
- 20 项 Playwright 回归通过（`experience`、`start-flow`、`city-startup`、`cloud`，单项上限 60 秒），覆盖桌面、手机、旧档迁移、备份、刷新幂等、覆盖／删除／多标签页和云档往返。之前覆盖存档测试在 30 秒上限下页面重载超时；单独复测 8.7 秒通过，整组 60 秒上限复测通过。
- 桌面与手机迁移截图已检查，显示 19 级、白丁 10 级，页面无横向溢出。
- 真实用户存档离线差异核对通过，结果见迁移规则文档；原文件未改动。
- 前端归档 SHA256：`53e7998f06142d7dd74592466facbd662198db1e9d6547976d7cb03f6567fb29`；云归档 SHA256：`7eddf076c364b8bafb0b1c8094688c4692ce6a508c17940babf2961171e9eb4c`，上传后均匹配。
- 服务器直接调用新 validator，验证合成 v19 的 99 → 19、职业 4 → 10 和二次校验不变。validator SHA256 与本地一致。
- 公网 HTTP／HTTPS 首页、主 JS 和 CSS 均 HTTP 200 且 SHA256 与本地构建一致；HTTPS 证书验证与 `/api/health` 通过。
- `egg-jianghu`、`egg-cloud`、Nginx 均 active，两应用 `Result=success`、`NRestarts=0`。

## 备份与回滚边界

- 切换前通过 SQLite backup API 保存 `/var/backups/egg-jianghu-cloud/pre-20260912_22ab00f.sqlite`，`PRAGMA quick_check` 为 `ok`，权限 600。
- 两应用分别位于原有根目录下 `releases/20260912_22ab00f`，`current` 已原子切换；各 release 的 `previous-release.txt` 保存原版本路径。
- 原静态服务器、云依赖与运维脚本继续复用，未改变数据库结构、SMTP、Nginx 或 5173 服务。
- 新档已为 v20，不能直接将前端／校验回退到只支持 v19 的旧版本。若需回滚其他功能，须保留 v20 读取能力；不以发布前数据库覆盖新增用户进度。
- 构建、部署临时包、真实存档及截图不提交 Git。
