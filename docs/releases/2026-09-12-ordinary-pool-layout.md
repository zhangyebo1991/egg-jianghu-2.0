# 普通池与界面布局发布记录

- 日期：2026-09-12；业务版本：`5018e4d`。
- 前端与云服务 release：`20260912_5018e4d`，上一版本均为 `20260911_b778c15`。
- 发布内容：统一普通池、世界解锁与去重、100 蛋蛋／1.2%／70 抽保底、赵云福利码，以及商城栏目切换和商城／背包／阵容／设置内容边距统一。
- 入口：`https://223.6.255.187`；保留 `http://223.6.255.187:5174`。

## 验证

- 686 项客户端单测、3 项云服务集成测试、生产构建通过；构建仍有原有大分包提示。
- 相关招募、购卡与页面边界 Playwright 回归已通过，覆盖桌面与手机。
- 上传归档和云校验文件 SHA256 与本地一致；公网 HTTP／HTTPS 首页、主 JS、CSS 与本地构建 SHA256 一致。
- 公网 TLS、`/api/health` 正常；`egg-jianghu`、`egg-cloud`、Nginx 均 active；两应用 `Result=success`、`NRestarts=0`。
- 远端使用合成存档验证 69 次保底进度、1234 蛋蛋保留，旧档缺失字段初始化正确；未写入玩家云档。

## 数据保护与回滚

- 发布前 SQLite backup API 备份：`/var/backups/egg-jianghu-cloud/pre-20260912_5018e4d.sqlite`，`PRAGMA quick_check` 通过。
- 前端位于 `/var/www/egg-jianghu/releases/20260912_5018e4d`，云服务位于 `/opt/egg-jianghu-cloud/releases/20260912_5018e4d`；各 release 的 `previous-release.txt` 保留上一版本路径。
- 同步重新构建云校验，未变更数据库结构、SMTP 或 Nginx 配置；保留原静态服务脚本及 5173 服务。
- 如需回滚界面，可原子切换前端 current 并重启静态服务。旧版云校验不认识本次新增存档字段，应保留本次云校验或准备兼容修复，不能直接回退后处理新档。数据库不随应用版本回滚覆盖。

- 公网隔离浏览器实测：桌面 1440 与手机 390 下，普通池首世界三人、商城栏目切换、收益卡显示和设置页正常，无页面异常或横向溢出。已核验线上桌面商城和手机设置截图；未注册账号或上传云档。
