# 蛋蛋收益与商城发布记录

- 日期：2026-09-11。
- 业务提交：`38e0536`。
- 前端：`/var/www/egg-jianghu/releases/20260911_eggs_38e0536`。
- 云服务：`/opt/egg-jianghu-cloud/releases/20260911_eggs_38e0536`。
- HTTPS：`https://223.6.255.187`；原 HTTP：`http://223.6.255.187:5174`。
- 本次发布包含蛋蛋在线／离线收益、独立商城、周月卡购买及生效、阵容加成明细，以及对应云存档校验模块。

## 验证

- 复用本轮已通过的构建与浏览器回归，发布时重新构建云校验并通过 3 项云服务集成测试。
- 上传前后归档 SHA256 一致；前端为 `8646519cff4848004290fc737b277e37baaec615266f0416ca9c07bd20c2bc3d`，云服务为 `cbada84cbd168e55657774f5286db307d3869d34404a8650aa64099eeb5e023d`。
- 远端模块直接验证非零蛋蛋余额、周月卡有效期、奖励尾数完整保留，以及旧档缺省初始化。未创建线上账号或修改玩家存档。
- `egg-jianghu`、`egg-cloud`、`nginx` 均 active；两个应用服务均 enabled。内网及公网健康接口通过。
- HTTPS 与原 HTTP 首页、主 JS/CSS 均 HTTP 200，内容 SHA256 与本地构建一致。
- 公网商城交互通过，零余额购买按钮正确禁用；桌面 1440×960、手机 390×960 截图已查看，文件为 `test-results/deploy-38e0536-{1440,390}.png`。

## 数据保护与回滚

- 切换前使用 SQLite backup API 创建 `/var/backups/egg-jianghu-cloud/pre-20260911_eggs_38e0536.sqlite`，`PRAGMA quick_check` 通过。
- 前端、云服务均由 `current` 原子切换；原版本均为 `20260911_cloud_2`，路径记录在各新 release 的 `previous-release.txt`。
- 未变更数据库结构、SMTP 配置、Nginx 配置或 5173 服务。
- 需要回滚前端时，将前端 `current` 原子切回上一 release 并重启 `egg-jianghu`。旧云校验不保留新增字段，应保留本次云校验或先准备兼容修复，不直接回退到旧校验处理新存档。
- 数据库备份用于灾难恢复，不随应用版本回退直接覆盖，避免丢失发布后玩家的新进度。
