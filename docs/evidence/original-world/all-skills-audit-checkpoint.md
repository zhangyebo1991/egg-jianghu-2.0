# 全技能原版核验 checkpoint

## 当前目标（2026-09-10 用户调整，优先于下方历史记录）

以本作设计为准，只修复明显影响技能效果或玩法的错误；不再要求与原版完全一致。下方历轮的“必须继续”“目标 active”和逐 ID 完全对齐要求属于历史标准，不再作为自动续审指令。

- 已完成实质修复：260 个敌人技能配置、376 条技能基础数据核验，以及技能等级、装配属性、主要伤害/治疗、耗能/冷却和关键特殊效果。土匪豪强应使用突刺Ⅰ与三连击，错误携带包扎已纠正。
- 已处理此前唯一明确必改项：移除敌方通过同编号我方角色副手“梵行无终”获得零冷却的串扰，以及专用于该关联的编号列表和字段；保留我方装备者及其召唤的正常效果。
- 整体复查发现的第 13 个及后续状态槽计时/持续效果问题已修复；所有已接纳状态正常计时与结算。当前没有其他已确认的重大待修问题。
- 停止将毫秒延迟、同帧实例顺序、原版宿主帧调度、完整弹体生成链和逐 376 ID 原版运行验收列为必做。已落盘的调度修改保留，不为简化目标批量回退。
- 逐 ID 报告中的 376 pending 表示旧标准下未完成逐技能原版运行验收，不表示 376 个已知 bug，也不阻塞当前目标。
- 最近验证记录：49 文件 531 测试通过；之后 UI 准备状态文字与行动者显示调整后构建通过。本次文档更新未重跑业务测试，全部业务修改仍未提交、未推送。

以下保留历史调查和证据，旧待办须按上述标准重新判断。

## 来源与覆盖

- 原版目录：相邻 `诸天刷宝录/_analysis`，jn/fw/buff/zh/zy/data.json 与 scripts/c3runtime.js。
- 原表共 376 条具名技能（包括 ID 0 防御）；此前生成器仅导出 337 条。已改为导出全部 376 条。ID 69 空行仍由历史引用触发跳过日志。
- `all-skills-runtime-audit.json` 保存治疗、护盾、伤害、技能初始化系数、技能核心、AI 和特殊机制的解码表达式、操作 SID 与路径。目前只保存了选中的函数，尚未逐项审核全部函数。
- 可复用 decoder：读取 `egg-jianghu/scripts/generate-original-world-evidence.mjs`，截断于 `const runtimeFunctionEvidence =`，追加自定义脚本；变量 runtimeData.project、namedEventNodes、runtimeOperationsIn、parameterExpressionsIn、functionCallsIn 可用。真正函数块需遍历 n[0]===4 && typeof n[1][0]==='string'。

## 已修改

- 新增 scripts/original-skill-initialization.mjs，复现数据处理function 对物理/法术技能的 jn[16]/[17] 初始化和能量×4秒冷却。技能生成与玩家升级目录共用。
- 初始化 jn[16] action SID 824264077027676；条件为 jn ID>0 且 jn[26] 为物理或法术。公式来自技能主动系数/种类系数/属性系数/范围分摊系数。治疗行为“生命治疗”乘0.55；包扎初始系数51%，成长10*(51/50)。
- 生成技能威力改用 round(jn[16]*jn[29]/100)，不再以 jn[29] 兜底100%。
- 治疗保留 healingStat：jn[26]生命用生命上限及sx112，否则用法攻及sx115。原版仅50自愈、331岩核复苏属于生命基数。
- support.ts 区分表达式10728生命分支（治疗加成独立乘）与10729法术分支（治疗加成并入元素/专精/熟练池）。
- 能量技能生成 energyGain=jn[25]，不再所有技能加1；推条 powerPercent=jn[23]，不再100%（或初始化后误为0）。原版技能释放核心的战斗获得能量/进度调用已保存在证据JSON。
- 更新旧治疗测试，避免生命与法攻都1000掩盖错误。回春术I系数55%，法攻1000×职业2得到1100，当前持续恢复22。

## 验证与限制

- 当前 npm test：47文件448测试通过；npm run build通过，仅既有大chunk提示。
- 这些测试不能证明全部技能原版一致；能量/推条尚缺针对不同数值的端到端回归测试。
- 生成器重切3张召唤图但无内容diff；6个召唤物原版动画帧缺失，当前有兜底立绘。
- 全部修改未提交、未推送。用户13张未跟踪heroes截图保留。

## 下一步（必须继续）

1. 用原版运行时表达式独立评估全376条初始化结果并建立逐ID对照报告/测试，检查物理法术以外字段及所有运行时变更，补源文件hash/可重复证据生成入口。
2. 核对技能等级：unitSkillById 当前仅玩家可学 martial 映射走升级；普攻fallback直接skillById，敌方技能等级未赋值。martialEffectAtLevel 当前round的位置也与原版不完全一致。
3. 核对所有行为状态机：攻击、防御ID0、治疗/护盾、复活、能量、推条、召唤、被动；重点特殊技能机制function 与技能核心中的分支，切勿用通用行为覆盖特殊规则。
4. 核对buff自附/目标附加/层数/概率升级/命中/消耗/持续时间/伤害或恢复快照；当前 passiveAttributes静态、chance静态等需逐项比对。
5. 核对目标选择、全36范围矩阵、随机/地面/自身、受伤目标限制、每次结算时机、冷却与消耗。已知冷却当前行动开始写入，原版Wait1.2秒后写入，尚未修。
6. 检查每种行为的端到端敌我回归、全量目录覆盖，再运行相关测试与构建；需要时UI检查升级数值显示。所有差异修完且逐项有证据后才能完成目标。

注意：上一条口头更新说373条为误数，当前已用原表完整计数确认为376（含防御）。

## 第二轮进展

- `scripts/check-original-skills.mjs` 独立执行证据中的原版表达式，不调用初始化模块的常数实现；376条初始化/威力/成长/冷却/行为/范围编号/治疗基数/能量数据全部一致。输出 `all-skills-data-audit.json`，含jn/fw/data/c3runtime源hash。该脚本尚未验证所有buff字段和状态机，不可据此结项。
- 技能表保留baseEffect、effectGrowthPerTenLevels、effectMultiplierPercent、buffChanceGrowthPerTenLevels。unitSkillById改为所有技能通用等级计算；玩家应用正数sx[75+category]额外等级，不受学习上限截断；敌人不加这个等级。fallback普攻使用同一入口。
- `buildCombatParty` 给职业普攻写当前职业等级。`martialEffectAtLevel` 按原版先除100再round，纠正原先保留两位小数。
- 原版 `刷新角色战斗属性` 证实敌人各技能槽等级固定1；`角色普攻编等` 证实普通普攻等级=当前职业等级；器魂普攻替换时等级=1。相关三个函数已追加至runtime证据。
- 450项测试通过、构建通过、diff-check通过。新增包扎等级10+额外2得到62%、敌方不受我方等级加成、推条不受等级影响回归。
- 仍须实现/审核：器魂普攻替换（原版遍历10装备位，最后一个替换覆盖前面；当前只取职业普攻）；技能栏与普攻若同ID但等级不同需确认是否实际可出现。
- 特殊机制已读出：jn[45]=1 清心61随机清减益；=2 复活64/171/216，以及**261落云回天阵**（当前只当自身回能，疑似漏复活）；=3全部召唤技能。当前尚未修这些分支。原版清心函数循环目标但读取数据阵位号，需核对参数语义，不能想当然按目标清状态。
- 继续上一节未完成的所有项；尤其敌方技能目录原版来自jnz[dr[8]]，现有enemies.ts主要从zy/js生成，需要全260敌人对照确认。

## 第三轮进展

- 已实现器魂普攻替换：stats.equippedBaseAttack按装备槽顺序扫描，最后一个普攻替换覆盖，初始等级1；卸下恢复职业普攻与职业等级。buildCombatParty使用此结果。
- 修复器魂“普攻替换”被误加为同编号属性：stats.ts两处只将kind=特殊词条的器魂加入属性。测试实际装备器魂45替换81并检查不额外增加sx81、卸下恢复，已通过。
- **确认260种敌人全部技能配置错误并修复**：原版刷新角色战斗属性由刷新普通关卡调用（SID401720848911197），敌人按jnz[dr[id,8],1..4]写入普攻和三主动槽。全运行时dr/jnz写入仅数值取整，没有启动后换成js/zy。generate-zhutian-enemies.mjs已改为统一jnz源，保留原成长/形象/掉落。
- 土匪豪强原版技能组6：普攻10突刺I、主动89；张角组2：普攻4、主动93火扇。此前对土匪包扎的原版量化分析前提不成立，虽然包扎自身法攻51%修复仍正确。
- `check-original-skills.mjs` 已增加全部260敌人jnz对照，报告追加敌人技能列表以及dr/jnz源hash；376技能数据和260敌人配置0差异。
- 451测试、构建通过。张角测试改为原版火扇3能量；实时补帧测试原先单次跨500秒、首战外挂战斗属性在重开丢失而战败，改为跨首胜利+4秒验证重启，避免敌人强弱混进重启测试。
- 重新生成260敌人图像无图像内容diff。全部修改仍未提交。
- 未完成：清心/复活261/召唤替换等特殊行为、所有buff与效果/时序完整审计、全范围与敌我行为端到端测试。下一轮继续这些，勿重复前三轮工作。

## 第四轮进展

- 清心61已实现：原版特殊技能机制接收施法者数据阵位号，按范围选中单位循环，但实际从施法者状态中随机删除一种减益的全部层数。61的range1为自身；测试保留增益、两种减益只清一种。
- 技能生成器保留specialMechanic=jn[45]。check-original-skills已追加该字段对比。
- 冷却改为行动结束1200ms写入；completeActionCooldown复用于通常行动、换波、结算过渡。原版全局变量“技能冷却最低值”为5，故缩减倍率clamp(100-sx37,5,100)，按100ms取整。测试300%以内大幅缩减仍留600ms（12s技能），1100ms尚无CD，1200ms写入。
- **buff时序**：原版buff命中计算先Wait1秒，目标buff/自附buff/增效消耗都在该节点。CombatActionPlan新增buffsAtMs=1000/buffsResolved；resolveActionBuffs集中应用目标buff与settleSkillStatuses，命中节点不再立即应用。通常行动与换波/结算过渡都推进到buff节点，避免杀敌换波丢掉自附buff。该修复同时补上护盾/能量/推条等行为之前遗漏的目标buff应用。
- 旧增效测试改为推进至起手100ms+等待1000ms；治疗测试明确1000ms绝对时刻尚无buff、1100ms才出现。453测试、构建通过。
- runtime证据追加角色行动function、召唤数量判定、替换召唤阵位、技能目标选择开始function。
- 261：数据为自身增加能量、range33、jn23=0、jn45=2。特殊复活分支还要求目标网格是尸体，不能仅见jn45就让它任意复活。尚需核对目标选择与原版该分支是否实际可达，未改成revive。
- 召唤自动AI原版上限时会禁用，当前一致；特殊函数保留手动替换最少剩余时间召唤物的路径。需判断目前无手动战斗入口是否有普攻召唤fallback触达替换，以及召唤属性/等级/时间全部比对。
- 尚未完成：buff获得/叠层/持续时间/快照与免疫等完整对照；攻击伤害和各行为端到端独立对照；全36范围矩阵；召唤与复活数据/时序；原版特殊分支可达性；清理生成器旧js/zy引用收集；完善可重复证据生成入口。不要仅凭453绿测结项。

## 第五轮进展

- 修复召唤模板六维错列：原版zh5生命/6速度/7物攻/8物防/9法攻/10法防；生成器输出格式[生命,物攻,物防,法防,法攻,速度]应取[5,7,8,10,9,6]。
- 原版刷新召唤物战斗属性对sx8/sx10均取主人法攻×zh9强度系数；现已修复双攻取法攻。系数先round两位，最终属性乘积**不取整**。原旧测试错误地断言最终取整，已换成不同物攻/法攻与浮点精度回归。
- 召唤物普攻等级=召唤技能有效等级。CombatSkillContent添加effectiveLevel运行时字段，召唤对象存skillLevels；unitSkillById对召唤物不再次叠加已包含的sx技能等级。召唤数据行0保留主人角色编号，所以附带主人职业系数和主手类型以复用原版职业/武器熟练结算。
- 召唤时间原版round(zh11*(1+sx33/100))秒，修正原先按毫秒round；30秒+25%得到38秒。
- 修复群体护盾上限读取对象：原版target生命×(1+**actor**的sx24/100)，最终护盾floor并限制上限；此前读target sx24。已加群体护盾回归。
- 获得buff叠层规则：3/4/5/65和6取更强快照，47不屈保留首次恢复快照；已修正47并补回归。全部技能带buff时jn24均>0，所以本轮没改applyBuff最小层数1。
- 原版中毒/流血/燃烧均创建zidan1并走子弹碰撞，均先扣盾，当前pulseStatuses方向一致。原版子弹碰撞进一步含命中/伤害/恢复/护盾等最终分支，已整段追加到证据，待完整核查。
- 独立核验脚本扩展：全部67buff（属性、极性、时间/回合、持续/层数、dot/hot种类）、9召唤模板、36×15×15范围矩阵及targetMode，全部0差异；含buff/zh源hash。
- 456测试、构建通过。runtime证据新增获得buff、失去指定buff、召唤时间/数量、buff伤害计算、流血/中毒/燃烧/恢复/生命恢复函数、子弹碰撞。
- 继续未完成事项：**优先核对子弹碰撞及伤害计算完整公式、复活生成我方战斗角色**，并检查生命恢复sx15是否实际脉冲（当前pulseStatuses只遍历statuses，疑似缺失）、各行为目标AI/随机地面范围语义、免疫/属性clamp；召唤fallback上限替换与器魂125最大数99/当前sx32+1差异；261原版尸体分支可达性；源证据可重复生成入口与逐技能覆盖报告收口。目标仍未完成。

## 第六轮进展

- 修正伤害公式错误封顶：原版公式各倍率只限制非负；我方角色刷新按sx[8]限制属性，受伤害118/119/120/123..130是负向下限，其余是正向上限。unitAttr现在按该分支处理，敌方与召唤物保留独立刷新行为。正向受伤害+200%完整生效，我方-200%限制至-95%，物理减伤200%限制至80%。
- 删除上轮误加到pulseStatuses的重复生命恢复；engine全局节点已先遍历全部单位恢复，然后结算状态。既有每秒先恢复100再受伤150回归恢复通过，不改变测试来迎合重复恢复。
- calculateModifiedSupportStat按原版先将面板攻防限制为非负，再乘修正及职业系数；补负面板回归。更新damage旧cap注释。
- 当前47文件459测试通过、构建通过（既有chunk提示）、diff-check通过；独立原版技能数据核验376条0差异。
- 全部修复未提交、未推送。全量行为目标仍未完成，下一步继续第五轮未完项：伤害/碰撞剩余分支、复活初始化、目标选择、召唤上限替换、261可达性、buff时长负修正、可重复runtime证据生成及覆盖报告。不能将本轮绿测作为全技能行为一致证明。

## 第七轮进展

- 上轮属于有效进展：属性限制与负面板修复落盘且通过回归。本轮续查原版实际函数，未重新从数据表开始。
- 自附状态原版无“sx34>0”条件，统一clamp(100+sx34,10,Infinity)/100；修复负时长修正被忽略。回归凝神箭专注30秒在-50%时为15秒，-200%时为3秒。
- **修复自附不屈缺失回血快照**：长生斩232、穿星刺233、万木破岳234的selfBuff47也经过获得buff首次恢复快照，当前此前只加防御不回血。首次ceil(0.2/10秒×法术治疗)；长生斩法攻1000、威力140%得到28/层。保留已有47叠层不覆盖首快照规则，新增真实技能调用回归。
- **群疗目标**：原版治疗目标存在判断只用于自动技能可用性，治疗核心从全部存活单位按生命比例最低者选，之后范围命中保留满血单位。因此将受伤过滤移到unavailableReason，群体治疗满血队友仍可得到附带状态。新增清泉沁心咒168范围/自动释放回归。
- **加能量核心目标**：原版生成我方战斗角色给战斗核心变量24“攻击”写max(物攻,法攻)，能量目标阵位号选其最高者并排除召唤。修复原先按普攻物法类型决定比较值，新增物理普攻但法攻更高的回归。
- **普攻召唤满额替换**：目标数据阵位号和特殊技能机制均有满额替换路径；自动AI仍禁用满额主动召唤，但普攻fallback可达。createSummonUnit现在只在我方满额时替换同主人剩余时间最短者，并清理其readyQueue，在其原阵位生成新召唤。验证保留另一主人的更短时间召唤物和自己的较长时间召唤物。
- **复活证据**：已提取生成我方战斗角色，HP确实round(maxHP×jn23/100)并clamp1..max，初始能量取zdls28、盾和行动进度初始化0，变量18为生命比例而非行动条。现有这些字段行为一致；刷新原始属性及初始buff/被动是否完整重建仍需继续核对，不据此认定复活全部一致。
- 261落云回天阵自动目标由“自身”前缀返回施法者数据阵位号，因而自动战斗不能选到尸体；手动地面选择是否可触发特殊复活仍需保留边界，目前没有手动入口，不改写为通用复活。
- **可重复证据入口已落盘**：从app运行 `node scripts/generate-original-world-evidence.mjs --skills-runtime`，只写技能runtime证据与source manifest，不重写城市/城镇业务文件。当前41个节点，manifest保存data.json/c3runtime.js SHA256。生成后check-original-skills仍376条0差异。
- 本轮完整验证：47文件465测试通过，构建通过（既有chunk提示），diff-check通过。修改均未提交/推送，13张用户heroes截图保留。
- 下一步仍未完成：伤害/子弹碰撞全部分支与工事加成、吸血/暴击负值和命中取整；复活刷新属性/初始化buff；敌方非攻击支持行为是否存在且目标分支一致；随机/地面目标及同值顺序原版证据；器魂125上限99；被动属性是否随技能等级变更；逐技能行为覆盖报告。继续核验，不以465绿测/376数据0差异结项。

## 第八轮进展

- 上轮属于有效进展，本轮继续碰撞结算原版运行时。新增普通伤害命中存活目标后清空全部剑势21；完全由盾吸收也清空，DoT碰撞不清。原版子弹碰撞SID943499551998929与父级生命>0条件为证据。
- 吸血原版读取同一“最终伤害值”：盾完全覆盖时保持原伤害，击穿时该变量改为ceil(max(1,原伤害-盾))。修复当前始终按原伤害吸血；伤害事件数值也按原版显示最终变量。回归850伤害、50%吸血，盾0/200/1000分别吸425/325/425。
- dealCombatDamage对击穿残余先ceil，再round剩余生命；修复召唤物浮点生命/盾场景，100.7HP、2.5盾受10伤得到93HP。暴击系数限制最低0（原版伤害计算clamp）。相关测试通过。
- runtime可重复证据扩展到48节点：直接/间接子弹生成、工事建筑加成、暴击/闪避、技能总属性、通用单条属性。直接/间接生成每目标一个弹体，jn36写变量6（碰撞判定帧）；没发现按jn36重复生成多次伤害。当前hits名义可能误导，但不能凭多段动画把整套伤害乘hits。还需核对子弹碰撞外frame启用条件以完成时序审计。
- 工事建筑加成(2)原版=(max(1,save[2,64,0])-1)*gs[2,4]/10；项目无工事状态字段，当前倍率1对应原版1级无加成。不要为了技能审计擅自建立完整工事子系统；需要明确目前可达状态边界。
- 复活初始化进一步查刷新角色战斗属性：清CD是有证据的，未见此函数直接添加初始buff；复活前属性从特定属性统计重新获取。当前永久属性在战斗中无写入，需结合技能装配属性修复后再审是否可复用快照。
- **新增高优先级未修差异：263个有名技能含jn8/9或jn10/11装配属性，绝大部分是主动技能。当前generate-zhutian-skills.mjs只给behavior=passive输出passiveAttributes，且把jn9/11品质系数直接当最终数值。** 原版技能总属性遍历12装备技能槽，等级>0且职业技能判断允许，按通用单条属性(属性id,品质系数,技能有效等级,"技能",jn6等阶,0)累计。主动技能一样生效。
- 通用单条属性“增幅”分支公式=round(100*等级*0.35*1.4^(等阶-1)*(sx3/100)*(品质系数/100))/100；sx131..136默认100。例野球拳42阶1、品质100、等级1=0.35%，当前漏掉。被动73阶8品质120应按此算，不能直接+120。混沌太玄经71是sx40乘法分支，需再提取通用属性等级转换、通用属性权重系数后修复。
- **下一步优先修技能装配属性全链路**：buildCombatStats只读心法且无技能131..136增幅，applyPassiveAttributes只在game-session build party和waves enemy生成处简单加值。需查特定属性统计如何使用sx131..136（多半加到6..11倍率，但必须原版证据），角色页面与战斗共用计算；避免修战斗而面板不变/双算。敌方原版刷新不调用技能总属性，当前给敌人applyPassiveAttributes也需审计。
- 当前470测试/47文件、构建通过（既有chunk提示）、diff-check通过；376基础技能表达式0差异，但该检查尚未覆盖263技能装配属性。所有更改未提交、13张用户截图保留。目标仍未完成；还需第七轮其余剩余项与逐技能覆盖报告。

## 第九轮进展

- 上轮有实际碰撞修复与新证据；本轮优先完成263技能装配属性链路。
- 生成器为全376技能保留skillTier=jn6，以及263条equippedAttributes（sxId/coefficient，原jn8/9与10/11）；删掉只限被动技能且把品质当数值的passiveAttributes字段。
- stats.ts新增skillAttributeValue：原版增幅分支round(100*level*.35*1.4^(tier-1)*(sx3/100)*(quality/100))/100；经验加成40的百分比技能分支相同但100替换1000。全表只使用40与131..136，未泛化未使用属性类型。
- equippedSkillAttributes按当前装配、学习等级>0、职业适用性累计；有效等级增加正数sx[75+skillCategory]装备加成。buildCombatStats核心6..11将对应125+id增幅与天资相加，先round再乘职业核心系数；buildAttributeMap补装配属性本身，并复用相同面板结果。
- 移除game-session与waves的applyPassiveAttributes旧加法，删除该函数。原版敌方刷新直接敌人属性+buff，不调用技能总属性；当前我方从面板统一计算，不再战斗重复相加。敌方被动槽也不因此平白获得我方装配属性。
- 新skill-attributes.test.ts读取runtime证据?raw，逐一用原始表达式计算263技能每条装配属性在1/10/50级值，对比运行时函数；野球拳42等级10得到物攻增幅3.5%，额外2级得到4.2%。装配/升级/卸下的面板与buildCombatParty双向回归通过。被动73阶8品质120一级是4.43%而不是120%；经验技能71一级为3.76%。
- 原版核心取整位置修复导致Lv1白丁真实内部面板保留小数：558.9HP/151.2速度/111.6双攻/55.8双防（UI可按原有格式展示）；旧测试按先乘职业再round的断言已按原表达式修正。
- 生成器修改曾误把buff.value也改为coefficient，全量数据核验发现59差异并定位；已定向恢复buff输出，全部buff对照恢复0差异，控制/不屈/换波回归恢复通过。没有为错误输出修改测试。
- runtime入口现51节点，追加通用属性等级转换/权重及特定属性统计；check-original-skills新增全量装配属性与等阶对照、计数263及sx.json源hash。
- 验证：48文件473测试通过；测试证据读取从node:fs改成Vite ?raw后focused3测试重验通过；构建通过（既有chunk提示）；数据376条含263装配属性0差异；diff-check通过。未提交/推送，用户13张heroes截图保留。
- 仍未完成：逐技能行为覆盖报告与未知项收口；jn36实际为弹体判定帧/动画hits，需查碰撞启用与多段时序；敌方支持技能目标分支；随机/地面/同值顺序；普攻标记非普攻开关与ID相同槽位；器魂125上限99；复活重新读取永久属性的可达变化（现战斗中装备快照不变）；免疫等buff完整规则及原版有限状态槽；核验主页面是否按已装配技能显示属性一致，必要UI验收。工事系统当前不存在，倍率1对应原版工事1级无加成，应在覆盖报告记录当前可达边界而非擅造子系统。不要只凭473绿测宣称全技能完成。

## 第十轮进展

- 本轮完成证据可靠性修复及验收清单，属于有效进展，未改业务规则。
- 原版条件格式与动作格式不同：条件inverted=node[5]，真实SID=node[7]；旧runtimeOperationsIn把所有SID取node3，条件SID因此全为0，并漏掉取反标记。现已修复，技能证据导出保留inverted。原始获得buff条件路径8.5.8.0.8.0.8.0.6.1为“zdls当前槽>0”且inverted=true，所以实际是找空槽；不能照字面误改为占用槽。
- runtime evidence重新生成53节点（追加战斗开始function、战斗数据生成），原版数据376条0差异、属性表达式重放3测试通过、diff-check通过。业务文件未改，沿用第九轮完整473测试/构建证据，不重复跑无关检查。
- 新docs/evidence/original-world/all-skills-coverage.md列出完整验收项与未覆盖边界；尚未建立逐ID行为关联表，不要误认清单存在就是目标完成。
- 获得buff中40/41/49/50四姿态互斥调用均失去其他姿态1层，当前原始maxStacks1，现有全部删除等价。该函数本身未发现控制免疫随机判定，不应凭想象加入免疫；仍需核查外围调用与完整取反条件。
- 状态容量调查：zdls是objectId37，原始project对象定义为['zdls',11,...,37,...]；未找到独立zdls.json。全局操作reference计数为47:32、48:18、34:99、31:2，没有显式SetSize证据；宽度应继续查布局全局实例初始化properties，不能猜20/30槽。战斗数据生成仅遍历队伍并调用刷新角色战斗属性，没有尺寸配置。
- 第九轮及coverage.md剩余项目继续，优先使用新证据复核取反条件（技能可用性/目标选择）；然后状态槽与普攻标记、器魂125等收口。用户13张heroes截图保留，全部未提交/推送。

## 第十一轮进展

- 复核自动技能条件找到并修复：原版自身增加能量仅当rangeId=1时检查满能量，群体范围不检查。此前123真武七星阵cost5永远被“满能量禁止”挡住，261落云回天阵同受影响。unavailableReason已加rangeId===1条件。
- 新回归123/261满能量可选、43单体回能满能量仍跳过；端到端123从5扣到0，再给自己及队友各回1点。48文件477测试、构建通过（既有chunk提示）。
- 自动选择敌人分支深层同样有能量>=jn19检查，角色行动双方均调用失去能量值；不要被浅层输出误导为原版敌人无消耗。当前普通敌方能量/CD约束方向正确。
- runtime证据追加失去能量值、器魂生效，共55节点。
- **新发现待修：器魂129源流不息的“不消耗能量”是失去能量值的显式分支，目前只当初始能量+5，实际仍扣能量。** 原版分支仅我方非召唤且角色编号>0生效；召唤和敌人照常扣。器魂生效函数只读wp.At(save.At(save.At(角色编号,15,1),1,3),27)，需确认save15对应实际装备槽（不要武断扫描所有装备替代原版指定槽）。同时用该证据处理器魂125召唤上限99。原版普通技能可用性仍先要求能量>=jn19，129只免扣费，不应跳过选择能量门槛。
- 上轮状态尺寸调查与本轮器魂槽位均未完成；优先完成器魂125/129指定槽生效，再推进coverage.md未知项。所有更改未提交/推送，用户13截图保留。

## 第十二轮进展

- 状态核实仍到第十一轮，上一轮已有业务修复；本轮修复器魂125/129原版指定槽效果。
- 原版物品装备函数明确写save[13+wp6]，器魂生效只读save15，所以实际为部位2副手。原wp1698丰穰神谕为器魂125，wp1712玄冥怒触为器魂129，均wp6=2。runtime入口追加物品装备function，现56节点。
- stats新增equippedOffhandSoulId从实际副手读取器魂编号，buildCombatParty将其快照存CombatUnit.offhandSoulId；召唤创建不继承该字段。物品入队、卸下回归通过，不扫描别的装备槽替代原版规则。
- spendSkill仅我方非召唤且offhandSoulId129时跳过扣费，仍先经过技能选择能量门槛；两种5能量释放cost3分别剩5/2，0能量且禁回能时129也只能回退普攻，均回归通过。
- summonLimit集中供技能AI与普攻召唤替换使用：我方非召唤的器魂125直接99，否则1+unitAttr(sx32)。**修正旧checkpoint的普通100上限说法：sx32本身上限3，所以普通我方99原始属性仍最多4，125才独立99。** 敌方不应用我方属性cap，且不给器魂125特例；物理格子15限制仍由空位判定生效。
- 当前48文件483测试通过、构建通过（既有chunk提示），所有更改未提交/推送，用户13张截图保留。剩余继续coverage.md，尤其状态槽/顺序、随机/地面和敌方支持目标、普攻标记与弹体判定帧、逐ID行为报告、页面验收。目标仍未完成。

## 第十三轮进展

- 第十二轮是有效业务修复。本轮找到zdls布局初始化，修复状态槽顺序。
- 原版project[5]为布局；主界面布局全局非视觉实例路径project[5][0][10][27]为[null,37,325,[],[],[203,31,11]]，所以zdls宽203、31阵位行、11数据层。获得buff只使用CurX>0，即状态槽1..202；67种buff不可能填满202唯一状态槽，无需猜20/30容量。
- runtime来源manifest新增statusArrayInstances，按zdls对象定义扫描布局、断言唯一，保存路径、实例ID325与dimensions；可重复生成而非手写尺寸证据。
- CombatStatus新增slot可选字段；applyBuff为初始未编号状态按顺序编号，新增状态复用最低空槽并按槽号排序，叠层保留原槽；满202槽返回null。所有正常状态都由applyBuff生成，槽号在删除其他状态后保留。
- 实际顺序回归：第一槽利刃过期、第二槽流血保留，然后恢复状态进入空闲第一槽；100满血先恢复无收益再流血30，结果70，旧追加实现会得到90。叠层不改变第二槽也已测试。
- 当前48文件484测试、构建通过（既有chunk提示），全部未提交/推送，13张用户截图保留。剩余coverage.md：随机/地面与敌方支持目标、同值顺序、普攻标记、动画碰撞判定帧、免疫来源收口、逐ID行为覆盖报告及页面验收。

## 第十四轮进展

- 第十三轮实际修复状态槽；本轮修复普攻加成判定。
- **纠正关键参数解释：伤害计算的第6参数是boolean“非普攻开关”，不是攻击次数。** 原版函数定义名/类型/默认值现导出到runtime证据parameters。子弹碰撞传jn27；DoT快照传1即禁用普攻增伤。此前章节若把jn27理解为次数，均以本轮真实签名为准。
- 当前executeAttack用skill.id===actor.baseAttackId判普攻，与原版不同。生成器全376保留nonBasicAttack=Boolean(jn27)，伤害使用!skill.nonBasicAttack；DoT/selfbuff快照原有强制false保持正确。jn1/10/81为0，42/89/112为1。
- check-original-skills追加全376的nonBasicAttack对照，0差异。新端到端回归：技能1放主动槽、普攻ID10时仍吃100%普攻加成，850→1700；技能42即使ID等于普攻也不吃加成。48文件486测试、构建通过（既有chunk提示）。
- runtime证据现56节点并保留函数完整参数签名，方便后续核对jn36判定帧而非盲目理解hits。临时_tmp-basic-marker.mjs已清理。
- 剩余coverage.md：敌方辅助技能目标与同值顺序、随机/地面路径、判定帧及动画碰撞时序、免疫来源收口、逐ID行为覆盖报告、页面验收。未提交/推送，13截图保留；目标未完成。

## 第十五轮进展

- 扫描原版全部260个敌人配置，非攻击技能仅16治疗Ⅰ与99治疗，均生命治疗/range1。check-original-skills新增enemySupportSkills报告，列出每种辅助技能及携带敌人ID；原版这些配置不携带召唤、复活、推进度、加能量支持技能，不应凭空添加敌方功能。
- engine.test新增两条敌方端到端回归：法攻1000、伤者100/2000、医者200/1000时，优先治疗伤者。能量3用99治疗1840，伤者变1940、能量0、行动结束CD12000；能量0回退16治疗550，伤者变650、CD0；我方与医者血量不变。
- 实际完整验证：48文件488测试通过，构建通过（既有chunk警告），独立核验376技能差异0。
- 同值顺序找到新的原版运行时证据：c3runtime.js的PickInstVarHiLow严格用v<bestVal或v>bestVal，同值不替换首个已选实例。当前targeting.ts以阵位号打破同值，不能据此直接认定等价；下一轮需核对生成我方战斗阵容/敌人/召唤实例的创建和复活顺序，再决定如何记录实例顺序。尚未修改目标选择业务逻辑。
- 其余coverage.md未完成项仍保留，特别是动画碰撞帧、随机/地面、免疫调用、逐ID覆盖与实际页面验收。全部未提交/推送；13张用户heroes截图保留。目标仍未完成。

## 第十六轮进展

- 上轮为有效进展：补充敌方治疗覆盖与实例选择器证据。本轮实际修复同值目标排序。
- 原版生成我方战斗阵容遍历save.CurX升序（C3 ArrForEach dims2为x从0到cx），调用生成我方战斗角色；该函数CreateObject战斗核心245。生成召唤角色同样CreateObject；复活再次调用生成我方战斗角色。敌方创建函数预创建15个核心、数据阵位16..30升序，生成敌方战斗核心仅填入数据，因此敌方阵位顺序可复用。
- 新CombatUnit.instanceOrder记录独立于阵位的实例顺序。buildCombatParty原版角色取definition.sourceId；自创角色无原版编号，明确排在原版角色之后并维持原阵位序，不声称其顺序来自原版。原版角色之间不受自创角色影响。
- selectLowestHealthPrimary与selectHighestAttackPrimary同值比较instanceOrder，省略字段的手工输入回退formationOrder。复活与新召唤赋为本阵营现存记录最大顺序+1，不改变战斗队列/阵位/范围遍历规则。
- 新回归覆盖治疗同比例异阵位/候选数组排列、回能同攻击及复活后失去优先权、新召唤低阵位不抢先治疗；现有复活端到端增加顺序1→3断言；GameSession覆盖后排主角sourceId1不被前排自创角色抢同值优先权。
- runtime证据增至60节点：生成我方战斗阵容、生成召唤角色、创建敌方战斗核心、生成敌方战斗核心；sources manifest保留C3 PickInstVarHiLow实际实现字符串。
- 492测试/48文件通过、构建通过（既有chunk警告）、376技能独立核验0差异。生成器临时scripts/_tmp-order.mjs已清理。
- 继续coverage.md：动画碰撞帧与多目标时序、随机/地面实际路径、免疫调用与取反、逐ID行为覆盖报告、实际页面验收。随机/地面调查仅读到攻击目标固定扫描与范围函数，尚未核对子弹分支，不标为完成。全部未提交/推送，保留13张用户截图，目标仍未完成。

## 第十七轮进展

- 上轮为有效进展，本轮实际修复3个复合技能和回能/推进度生效时点。
- **纠正此前jn36“判定帧”解释**：原版zidan(object255)实例变量6真实名为“分段数”，只找到直接/间接子弹生成写入clamp(jn36,1,Infinity)。本轮全事件参数扫描未发现读取zidan.分段数、CompareFrame或SetCollisions控制命中。zidan本体仅一个Animation1占位帧，实际动画在zidan特效(object256)。不要再用动画帧推导该字段，也不乘hits重复扣血。
- 子弹动画证据已加入可重复入口，现61节点。直接子弹生成在目标核心坐标创建zidan并禁用Bullet；间接子弹生成从施法核心Spawn并启用Bullet、朝目标坐标飞行。当前直接300ms路径方向有证据，远程300ms却未计飞行时间，仍需核对初始Bullet速度、核心/弹体尺寸与坐标、碰撞范围及是否越过行动结束；尚未修复远程时点，不标为完成。
- 原版技能释放核心分支：256回源术在生命治疗分支立即战斗获得能量(jn25)，另一个无条件子节点Wait0.3后生成治疗弹体；257回春斗气诀/258斗气愈灵阵在增加能量分支立即回能，再Wait0.3生成治疗弹体。之前只按单一behavior执行，漏256回能与257/258治疗。
- 生成器为256输出energyGain、257/258输出healingStat，独立检查扩展至这些复合字段。prepareAction扣费后立即执行所有grant-energy及256的回能，所有advance-gauge也立即执行；命中阶段去掉重复回能/推条，并为257/258补治疗。
- 新参数化回归：法攻1000、500HP时，256/257/258耗费后立即能量5/3/2，施法后299ms不回血、300ms分别治疗940/2920/3770且不重复回能。217推条施法瞬间350、300ms后仍350。全部496测试/48文件通过，build通过（既有chunk警告），376技能独立对照0差异。
- 清理生成器旧的js按同名boss/zy技能收集路径及无用源读取：全376具名技能已全量遍历，不再从错误敌方技能映射加入空白69。再次生成376数量不变，空白69警告消失。
- 临时scripts/_tmp-projectile.mjs已清理。剩余仍包括远程弹道与随机快照时序、随机/地面选择、免疫调用与条件、逐ID行为覆盖、页面验收。未提交/推送，13张用户截图保留，目标仍未完成。

## 第十八轮进展

- 上轮实际修复复合回能治疗，本轮继续原版弹道，完成有证据的几何延迟与随机快照修复。
- 原版布局初始zidan255实例尺寸100×100、中心原点，Bullet properties[2000,0,0,false,true,true,true]：速度2000、加速度/重力0、旋转开、步进开；无SetSpeed覆盖。战斗核心245尺寸150×150、中心原点；二者Animation1碰撞多边形为空，按矩形处理。生成阵位：我方x=-780+160col-60row，敌方x=780-160col+60row，y=40+170row（相对中点）。技能释放动作仅临时±10x、200ms复位，所以300ms生成弹体已回原位。
- 新projectiles.ts按旋转的100方形弹体与150轴对齐核心的分离轴计算首次接触距离/2000。原版像素坐标用于模拟，不依赖当前UI缩放。225对阵位双向对称回归，飞行77.5..837.5ms，加300ms全部小于1200ms行动结束。
- CombatActionPlan.projectileHits在300ms生成，按原版先暴击后闪避分别锁定boolean；executeAttack在各目标到达时读取当前伤害面板/暴伤倍率。近战同样使用生成时结果并当即碰撞，不按hits乘伤害。Scheduler改为各未处理节点最小时间，允许1000ms附buff先于远目标命中；全部目标命中后再标hitResolved。
- 实际回归：冰击双方核心相距920，300ms发射、397.5ms飞行，在总时797.5命中。生成后把暴击率改0、目标闪避改100仍保留原判定，暴伤改300则命中伤害2850，证明只锁定判定不锁最终倍率。破魔阵134的2目标近者先命中、仅各一次伤害，1200ms正确收口CD8000。
- 原护盾回归用远程敌人，原等待500ms不足以命中；改为1300ms观察完成动作后的盾/血结算。其他断言未放宽。完整49文件499测试通过、构建通过（既有chunk警告）、376技能独立对照0差异。
- 原版runtime入口增至65节点，新增生成阵位、目标位置坐标X/Y、技能释放动作；manifest新增4种projectileGeometryInstances的size/origin/behaviors。临时scripts/_tmp-flight.mjs已清理。
- **精度边界**：当前实现连续几何首次接触；原版C3 OnCollision逐帧并有Bullet stepping，其实际帧量化误差仍需原客户端实机对照，不能声称像素碰撞时间已逐帧验证。原版角色动画是否还有其他修改核心坐标/尺寸调用也需最终写入审计。
- 下一步继续：随机/地面路径、免疫调用和完整条件、逐ID行为报告、页面与原版运行时验收（含上述碰撞帧精度）。目标仍未完成；未提交/推送，13张用户截图保留。

## 第十九轮进展

- 上轮实际修复远程飞行，本轮核对状态/控制外围，修复已满行动条被额外控制拦截。
- 原版战斗行动积攒通过sqrt(zdls7*clamp(1+zdls113/100,0,Infinity))加进度，出手按行动权重/存活判定，没有再次以控制状态拦截已获得的行动机会。获得buff也未重置行动进度/权重。移除engine两处enqueue/take中的!isControlled，保留速度修正导致0速度停止积攒。新增眩晕gauge0不行动与gauge1000仍行动、状态保留的参数化端到端回归。
- 获得buff及buff命中计算完整条件/调用已复核：技能概率、目标选中/存活/阵营、时长、叠层与快照之外未见控制免疫抽签。不要把自创controlResistance或旧stat-descriptions.ts中的控制抗性介绍当原版规则。后者说明是否仍在当前UI使用应在页面验收核查。
- 修复证据提取器可靠性：runtimeOperationsIn此前仅凭前三项整数匹配reference，误把表达式元组识别成SubInstanceVar等动作。现在条件必须.Cnds.并符合boolean flags/长度、动作必须.Acts.并符合node4=0/node5=null形状；正确保留条件SID和inverted（包括无参数条件）。重新生成证据后，装配属性原始表达式重放3测试仍通过。
- 范围调查：原版范围35名随机，但全376具名技能没有任何jn37=35。check-original-skills新增rangeUsage记录每个36范围的全部技能ID。技能范围刷新→刷新技能范围数据→技能范围阵位号按核心方阵1..15或16..30查询fw矩阵，并把结果1/2置选中；没有额外随机抽签。自动目标数据阵位号按技能行为决定核心，地面标签主要供手动选择位置提示，当前自动路径与矩阵选择一致。手动施法未实现及261地面特殊复活保持边界，不自行另加随机。
- runtime证据现68节点，新增战斗行动积攒、技能范围刷新function、刷新技能范围数据。完整49文件501测试通过、构建通过（既有chunk警告）、376技能独立核验0差异。
- 临时scripts/_tmp-immunity.mjs已清理。剩余优先：逐ID行为关联报告，最终技能相关运行时字段写入审计，复活永久属性变化边界，页面验收/原版碰撞逐帧误差。所有更改未提交/推送，13张用户截图保留，目标仍未完成。

## 第二十轮进展

- 上轮实际修复控制拦截，本轮完成技能表运行时写入审计及逐ID证据关联产物，属于有效证据进展；未改战斗业务。
- 全project[6]按正确事件形状扫描jn写入，仅5处：JSONLoad、载入后jn[CurX,CurY]整数转换，以及初始化16=技能主动系数、17=10*(16/50)、20=19*4。载入转换排除字段1/2/14/15/18/26/28/30/32/34/39/40/50；原始jn所有数值本身为整数，因此无额外数值偏差，16/17/20已经由当前初始化器覆盖。未发现战斗期另行修改jn的操作。
- runtime sources manifest新增skillTableWrites（全5操作，含path/SID/表达式）与skillLoadOperations（完整载入条件），由--skills-runtime可重建，不用手工名单代替搜索证据。
- 新scripts/generate-skill-behavior-coverage.mjs生成docs/evidence/original-world/all-skills-behavior-coverage.json，每个376技能关联原版行为、范围、携带敌人配置、所需原版函数、明确边界与pending项。按复合256/257/258、装备属性、附加状态、特殊机制、召唤/复活等补充证据；断言每个函数存在、ID唯一、总数等于独立核验。
- 该产物刻意保留accepted=0/pending=376：它证明逐ID证据可定位，不证明逐ID运行验收全部完成。后续应据实际验收逐项补充可验证结果，而非简单把有函数引用等同完成。
- 已执行--skills-runtime、check-original-skills（376/0差异）、generate-skill-behavior-coverage（376记录），diff-check通过；本轮只变证据脚本/文档，沿用上轮501测试与构建证据，不重复跑业务测试。
- 临时scripts/_tmp-writes.mjs已清理。剩余优先：实际角色页面/技能装备属性验收、原版碰撞逐帧误差验证、复活永久属性重读边界、逐ID运行入口最终复核。还需关注动作是否写核心几何、当前UI控制抗性旧说明是否可见等已有未知项。所有更改未提交/推送，13截图保留，目标未完成。

## 第二十一轮进展

- 上轮是有效证据进展，本轮从main.ts页面操作和GameSession快照查到并修复复活不重读永久属性。
- **纠正前期假设**：main.ts在战斗中可通过equipMartial/equipEquipment等修改session.state，没有战斗锁；buildCombatParty仅在开战构造快照，因此“永久属性在战斗中不变”不成立。原版生成我方战斗角色复活会重新读取特定属性统计，当前沿用旧maxHP/攻击/技能等级产生偏差。
- createCombatEngine新增可选refreshPartyUnit回调（独立引擎输入不强依赖持久状态），仅在复活时调用，GameSession初次开战和自动重开均接入。回调由当前hero职业、装备、技能生成新快照；executeRevive先刷新再按jn23生命比例恢复、清CD/状态、赋新实例顺序。
- 回调构造临时单人formation保留本场位置，避免角色从页面编队移除后无法读取属性；**编队变化与原版复活位置是否一致仍须核实**，不把当前保留位置当原版已验证规则。报告相应pending改为位置规则，属性重读已有真实回归。
- 新GameSession端到端：开战后主角阵亡，持久状态升到10级并装备10级野球拳，旧战斗攻击保持不变直到复活。另一侠客施放64后，主角按当前maxHP×20%恢复，攻击/attributes/技能列表/42等级全部等于当前buildCombatParty。最初fixture用了legacy GameSession.create无主角，已改createNew真实新档，未修改业务迁就测试。
- 完整49文件502测试通过，构建通过（既有chunk提示），376技能独立核验0差异。未做浏览器页面验收，不用单测代替实际UI结果。
- 下一步：页面装配属性和技能效果说明验收、原版运行碰撞误差、复活/召唤等特殊机制完整范围边界、逐ID运行验收。全部未提交/推送，13张用户截图保留，目标仍未完成。

## 第二十二轮进展

- 延续game-playtest页面验收，真实Edge/Playwright完成装配、替换、卸下、筛选、保存、战斗锁定及1920/1440/640/390视口检查；已实际查看修复后的390长截图，未发现横向溢出或卡片重叠。
- 修复hero-martials.ts未替换buff几率占位符，使用martialBuffChanceAtLevel按等级生成概率。页面回归增加占位符不得残留断言。
- 新增实际基础面板回归：通过按钮装配10级野球拳，物攻显示高于原值；卸下后恢复原显示值。运行npx playwright test tests/e2e/hero-martials.spec.ts -g '武学装配'，2条通过。
- **纠正第二十一轮可达性表述**：main.ts领域处理器没有统一战斗锁，不等于所有页面允许操作。武学页明确禁用战斗期装配/卸下，且已浏览器验证；该轮复活测试直接操作领域状态，不能宣称已证明玩家可在页面战斗中换武学。装备页换套按钮无战斗disabled，装备与升级完整可达链仍待验证。
- check-original-skills本轮重新执行376条、0差异；git diff --check通过。概率UI修改后构建在上一段执行已通过；业务测试沿用第二十一轮502条，本轮不重复全量运行。
- 旧控制抗性说明定位：STAT_DESC按data-stat-label展示，当前属性表没有同名控制抗性项；但APT_DESC定力包含控制抗性旧公式，heroes-page.ts仍渲染定力雷达标签，不能视为整段旧说明不可达。待核对并修复真实可见公式。
- 武学页效果目前按学习等级展示，战斗会叠加正值sx[75+技能系]；有装备等级加成时的显示差异待修复和覆盖。当前尚未改此部分。
- 剩余：有效技能等级与属性提示、原版逐帧碰撞精度和核心几何写入、复活位置/261与召唤目标时点、逐ID运行验收。所有变更未提交/推送，13张用户heroes截图保留，目标未完成。

## 第二十三轮进展

- 第二十二轮为有效页面验收。本轮修复两类实际差异：武学有效等级说明、复活主目标顺序。
- 武学页接入buildAttributeMap，以正值sx[75+技能系]计算效果说明和搜索文本，并显示等级加成与有效等级；保留已学等级/学习上限标签。martialEffectAtLevel与martialBuffChanceAtLevel不再截断到学习上限或取整有效等级，学习入口自身仍约束合法学习等级。
- 全MARTIALS_V10逐项在maxLevel+2对照unitSkillById：带数值占位的效果与带buff几率的概率全部一致。没有数值占位的状态技能不以powerPercent验收说明（临战激励没有数值说明，其兼容power与战斗0系数不应混为一谈）。
- 真实Edge页面使用原有通用残记器魂26（技能系+5），30级野球拳显示生效35级/218%；实际查看卡片截图，无重叠。npx playwright test tests/e2e/hero-martials.spec.ts -g '器魂|武学装配'共3项通过。
- **纠正旧复活顺序结论**：原版生成阵位升序创建网格；复活目标阵位号ForEach遍历网格，对每个符合阵营/角色编号>0/非存活的网格执行SetFunctionReturnValue，没有StopLoop。C3 System.SetFunctionReturnValue仅写帧返回值，不退出循环，因此最终最大阵位生效。旧selectFirstFallenPrimary及旧测试关于最小阵位的假设错误。
- 改名selectRevivePrimary并选最大阵位，增加候选数组乱序回归和实际引擎双阵亡回归：阵位15复活400/2000，阵位2仍阵亡。原版forEachSource/functionReturnActionSource已加入可重复runtime sources manifest。
- 本轮完整49文件504测试通过、构建通过（既有chunk提示）；--skills-runtime生成68节点，独立核验376条0差异，逐ID关联报告重新生成。未将函数关联标记成全部运行验收。
- 下一步仍需：复活永久编队变更后的角色位置（原版生成我方角色读save，当前保持本场位置）、261自动与手动边界、召唤目标选择时点、原版碰撞逐帧和核心几何写入、可见定力旧控制抗性提示。全部改动未提交/推送，13张用户heroes截图保留，目标仍未完成。

## 第二十四轮进展

- 第二十三轮有效修复等级说明及复活目标。本轮发现并修复证据提取遗漏异步Wait，随后修复特殊技能生效时点和自动满额召唤行为。
- runtimeOperationsIn过去只接受动作node[4]===0，原版System.Wait的真实元组为[-1,45,null,SID,2,null,params]。现仅对System.Wait额外接受标记2，保留其他动作/条件形状检查防止误识别表达式。生成时断言角色行动Wait1.2存在；所有导出操作保留objectId/objectName，避免混淆核心、模型、网格。
- 完整调用顺序证明：角色行动直接调用技能释放核心、buff命中计算、特殊技能机制；普通伤害/治疗/护盾在核心函数自己的分支Wait0.3，特殊技能函数没有Wait。因此清心、复活、召唤应施法瞬间执行，旧统一300ms错误。prepareAction直接执行specialMechanic，标记命中已处理；传递rng/复活刷新回调到初次积攒、前行动结束接续和换波接续路径。首次构建查出两个接续调用漏传参数，已补齐并重新全量验证。
- 回归：复活64、召唤72在首个100ms积攒节点开始动作时就产生唯一事件，action.elapsedMs仍0；再推进300ms不重复生成。清心在相同时点移除一种减益，300ms不再移除第二种。
- **纠正第十二轮满额替换结论**：目标数据阵位号在自动召唤满额时选择替换召唤阵位；特殊技能机制先检查该网格角色编号不大于0且存活标记false。生成召唤角色明确写网格角色编号=主人、存活true，所以自动满额选中已有召唤格会被外层条件拦截，旧直接替换不等价。真正的满额销毁分支需手动选空格，当前没有手动入口。createSummonUnit自动满额返回null，回归保留两只自己的召唤物及别人的召唤物、无summoned事件。
- 生成coreAndProjectileGeometryActions全事件定向扫描核心245/弹体255的几何相关动作，目前直接核心变换仅12次±10的SetX（技能起手/受击等），未发现直接SetSize/Scale。对象family间接动作及原客户端逐帧误差仍未验收，不据此称全部碰撞完成。
- 新发现明确待修：生成召唤角色的核心maxHP=Math.round(召唤系数(...,6)*特定属性统计(...,6))，而刷新召唤物战斗属性的数组数值保留小数；当前maxHp和attributes6共用浮点，旧148.47核心生命断言不代表原版已验证。召唤系数还读取永久召唤强度，而部分数组系数读取当前zdls；下一轮需要分离核心/数组与永久/临时来源，不只盲目加round。
- 原版复活生成位置确实读save[角色,5,1]；当前编队页面与handler未统一战斗锁，当前回调保持旧位置仍待按实际可达场景修复。尚未改动位置业务。
- 完整49文件506测试通过、构建通过（既有chunk提示）；runtime68节点、独立376条0差异、逐ID关联报告重建，新增召唤明确pending。两个_tmp文件已精确清理。全部未提交/推送，13张用户截图保留，目标未完成。

## 第二十五轮进展

- 第二十四轮有效修复特殊技能时点与自动满额行为。本轮修复召唤出生核心生命、持续时间永久属性来源和初始能量。
- 原版生成召唤角色核心maxHP=Math.round(召唤系数(...,6)*特定属性统计(...,6))；召唤系数的生命分支按永久sx31取两位系数，召唤时间按永久sx33。刷新召唤物战斗属性的sx6/8/10却取主人本场zdls及当前召唤强度，不应共用核心值。
- createSummonUnit复用已有GameSession当前角色读取回调，单独计算permanentHealthCoefficient和取整maxHP，数组生命保留原浮点healthAttribute；持续时间用永久sx33。无持久状态的独立引擎回退actor.attributes（无buff），保留独立测试输入边界。GameSession读取方法更名currentPartyUnit，仍保持当前战斗位置，位置规则未宣称修复。
- 原版生成召唤角色直接写核心能量0，旧继承主人sx28错误。现在energy0但attributes28仍继承主人数组，避免把“出生能量”与“初始能量属性”混淆。
- 原148.47核心生命测试更正为maxHp/hp148、attributes6仍148.47。新独立引擎回归：永久生命201/强度20/时间50，本场生命101/强度13.37/时间25，得到核心314、数组148.47、时间45000ms、能量0而attributes28为5，主人核心101不被覆盖。
- GameSession回归：战斗开始后提升当前职业等级再施放72，召唤核心按当前永久生命*1.3取整，数组仍按主人旧战斗生命*1.3；初始测试只提高角色level不改变生命，已改提高当前职业level使差异真实存在，未修改业务适配fixture。
- 新定位状态刷新链：原版组刷新角色战斗属性function包含第二个函数“刷新战斗核心数据”，它按目标核心类型分流到刷新召唤物战斗属性(...,"刷新")。已加入runtime提取入口，后续核对获得/失去buff/脉冲等调用点及同步核心生命；不把出生修复当全生命周期完成。
- 完整49文件508测试、构建通过（既有chunk提示）；runtime现69节点、独立376条0差异、逐ID报告更新召唤pending。临时_tmp-summon-refresh.mjs已清理，diff-check通过。
- 继续目标：召唤状态刷新、复活永久编队位置与261、逐帧碰撞实机精度/间接family几何动作、定力旧控制抗性说明、逐ID运行验收。所有改动未提交/推送，13张用户截图保留，目标仍未完成。

## 第二十六轮进展

- 第二十五轮有效修复召唤出生属性。本轮继续状态生命周期，实际修复召唤物状态改变后不重读主人战斗属性。
- 原版全事件调用刷新战斗核心数据共5处：生成我方角色后的刷新入口、回合buff冷却、获得buff、失去指定buff、冷却及状态时间计算。失去指定buff只有层数清零后调用刷新，非清零减层未直接调用。sources新增statusRefreshCalls保留全部5处；runtime新增回合buff冷却function/冷却及状态时间计算function，现71节点。
- 召唤实例新增可选summonSkillId，对齐核心记录的召唤技能编号；无模板来源的手工fixture不凭名称猜模板。提取summonCombatAttributes供出生与状态刷新复用，保留双攻读主人法攻、生命/双攻强化系数2位、其他系数直接zh/100。
- 新refreshSummonAfterStatus在实际获得状态、状态耗尽/计时到期/回合到期、剑势清空、清心清空后重读主人本场属性。保持召唤实例、能量和剩余时间；更新数组与核心生命上限，刷新模式不取整，生命不自动增加但超新上限会截断。自己的buff仍由unitAttr聚合一次，未重复叠加到数组。
- 回归：召唤出生131/数组131.3，主人生命改201/法攻202但召唤暂不变化；召唤施放105获得状态后maxHP261.3、法攻141.4、HP仍131；主人生命再改51，状态100ms到期后maxHP/HP66.3，能量不变，剩余时间仅正常少100ms，实例仍1个。
- 完整49文件509测试通过、构建通过（既有chunk提示），独立376条0差异，逐ID报告重建；临时_tmp-refresh.mjs已删除，diff-check通过。
- 明确剩余：原版同一批多个状态移除可能逐个触发刷新，当前时间/回合删除统一筛选后刷新一次，需核对有无生命中间值差异；主人阵亡后zdls是否清零决定召唤重读来源；非清零减层的原版属性刷新是否另有入口仍待审计。不要把本轮单次状态回归等同所有状态生命周期完成。
- 另继续复活编队位置/261、原版逐帧碰撞和family几何、定力旧说明、逐ID验收。所有更改未提交/推送，13用户截图保留，目标仍未完成。

## 第二十七轮进展

- 第二十六轮有效修复召唤状态刷新。本轮修复计时槽范围、全阵位倒计时顺序、主人阵亡后召唤读取数据来源。
- 原版冷却及状态时间计算内层Repeat12，X次数从0逐次加1，只处理1..12槽；回合buff冷却用ArrForEach遍历全部槽。advanceStatusDurations现仅对slot<=12时间状态减时，无编号手工fixture按数组位置+1；回合逻辑不缩减范围。回归槽12到期、13保留原时间、14回合状态正常移除。先前“202可用槽等于202计时槽”不能成立。
- 原版Y次数按1..30处理倒计时；当前activeUnits先人物后召唤会改变同时到期时主人属性读取结果。tickRealtime改按本地阵位+敌方15排序。回归前排召唤与后排主人同时到期，召唤先重读主人仍存的利刃+20，主人随后状态清空，召唤数组保留+20直到下一次刷新。
- 核心阵亡中的我方普通人物仅销毁核心/形象并保留网格角色编号、zdls；召唤超时调用的销毁阵位角色则清数组与网格，不能混用。markDefeated为普通我方保存defeatedAttributes（死亡时已结算属性含buff），召唤刷新对阵亡主人读取该快照；复活清除快照。回归主人利刃+流血后由真实脉冲击杀、另一侠客存活，召唤施放105刷新得到主人+20与自身+20合计40。
- 关于单个单位同批多个状态移除：已核对全67buff没有直接sx6/31/33修正；刷新召唤生命为主人值+自身buff6（当前始终0），同一单位移除间无其他单位倒计时，因此现统一筛选后一次刷新不会产生额外核心生命中间值损失。跨单位顺序不能同样忽略，已实际修复并测试。
- 原版核心阵亡/销毁阵位角色function加入runtime证据，现73节点。全部49文件512测试、构建通过（既有chunk提示），独立376条0差异，逐ID报告将已处理的主人/顺序pending换为部分减层及最终生命周期复核。临时_tmp-death.mjs已删除，diff-check通过。
- 剩余关注：部分减层时原版zdls是否暂保留旧聚合值（失去指定buff仅清零时显式刷新）；DoT/HoT自己的槽遍历范围也要独立检查，不能从倒计时Repeat12推断；我方实际阵亡判定调用时点、复活编队位置/261、逐帧碰撞/family几何、定力旧说明、逐ID最终验收。所有改动未提交/推送，13张用户截图保留，目标未完成。

## 第二十八轮进展

- 第二十七轮有效修复计时范围、顺序及主人阵亡快照。本轮独立复核持续效果，修复脉冲槽范围和跨角色回血/持续效果顺序；收口部分减层不可达性。
- buff伤害计算function自己的内层也是Repeat12，不能从数组203推导遍历202。pulseStatuses仅处理slot1..12，新增12槽流血生效而13槽中毒/14槽恢复不生效回归；原状态保留且不凭空移到前槽。
- **纠正早期全场先回血表述**：buff伤害计算的Y次数1..30循环体先生成该角色生命恢复弹体，然后生成该角色前12状态槽的DoT/HoT弹体，再处理下个角色。tickRealtime改为按阵位逐角色回血→持续效果；旧单角色测试无法证明跨角色顺序，已改为两角色四事件顺序断言。原版这些持续效果使用zidan1、禁用Bullet，在目标点生成；逐帧碰撞量化仍属于总体运行时精度边界，未实机确认。
- 生命周期部分减层已收口：全project[6]的失去指定buff调用16处，参数为清心/剑势99、jn43、姿态40/41/49/50各1。全部376具名技能的jn43对应enhanceConsumeStacks只有0/99；4种姿态原版/生成表maxStacks均1，所以这些路径不会出现减层后剩余非零状态，不需要另增属性缓存。sources新增buffRemovalCalls保存全部16调用。
- runtime新增生命恢复/流血效果/燃烧效果/中毒效果/恢复效果5节点，现78。完整49文件513测试、构建通过（既有chunk提示），独立376条0差异，逐ID报告移除已证明不可达的部分减层pending并添加明确边界。临时_tmp-periodic.mjs已清理，diff-check通过。
- 剩余重点：复活编队位置/261、核心阵亡实际调用时点（尤其行动锁中碰撞）、原客户端逐帧/step碰撞及family间接几何、定力旧说明、逐ID最终验收。召唤全生命周期已核对的分支见前轮，不据此将所有技能accepted改绿。所有更改未提交/推送，13用户截图保留，目标仍未完成。

## 第二十九轮进展

- 从第二十八轮恢复，已核对当前Git状态；13张用户截图保留，未提交/推送。
- **纠正早期命中立即阵亡结论**：全project[6]扫描核心阵亡函数调用仅1处，位于未暂停、战斗开关、地点战斗开关且!角色行动条件下。碰撞及持续效果没有其他直接调用；核心阵亡组内同名真正函数已独立展开核对。sources新增deathCheckCalls保留唯一路径和SID。
- resolveActionHit不再直接settleDefeatedUnits/settleBattleState；finishActiveAction在1200ms写CD并解除行动后判死，再检查换波/胜负，最后决定是否继续队列。积攒阶段脉冲判死保留，行动阶段原本不走tickRealtime。
- 修复后的致死命中保留alive核心、状态与队列至行动结束；收益与unit-defeated只在收尾发出。更正5条旧错误预期：闯荡最终胜利1200+800=2000ms，驻守1200+1000=2200ms；100ms起手的普通战斗1300ms判死、1800ms换波刷新、2300ms恢复。同批多个目标1200ms结算。原客户端逐帧事件先后仍属未实机验证范围，勿把连续模型时点当帧级等价。
- 完整49文件513测试通过、构建通过（既有chunk提示）；重新生成78runtime节点，376数据0差异，逐ID仍376pending。已更新覆盖表中陈旧的部分减层和状态槽描述。
- 下轮：复活编队位置/261、原客户端逐帧碰撞与family间接几何、定力旧说明、最终逐ID验收；也需核对核心阵亡遍历1..30与当前activeUnits遍历顺序是否影响收益随机种子及召唤快照。目标未完成，不据绿测收口。

## 第三十轮进展

- 上轮为有效修复。本轮继续原版编队/复活证据，并修复阵亡遍历顺序和可见定力控制抗性旧说明。
- 原版核心阵亡Repeat30按数据阵位1..30扫描；settleDefeatedUnits旧activeUnits按数组顺序，可能让同批击杀掉落种子分配给不同敌人。现按formationSlot+敌方15排序。新增双敌真实DoT脉冲击杀，重排enemies数组仍按front/rear给出完全相同收益事件和seed。最初fixture只置HP0被原生回血救回，已改真实DoT击杀，未改变业务迁就测试。
- 提取器新增callsWithConditions，sources中deathCheckCalls保留完整祖先条件，断言仅1调用且角色行动取反。原runtime按同名真实函数优先，单看核心阵亡函数本身不足以证明外部gate；现在可重复生成其调用条件。新增formationEditorCalls四个入口及祖先条件，runtime增加阵位交换function、人物下阵function、人物上阵点击，现81节点。
- 原版四个打开阵型编组入口的祖先事件条件未见战斗开关/角色行动限制；交换函数直接写save[人物,5,1]，下阵写0；还需核对主要按钮编组在战斗页面是否生成/启用，不能将handler无锁等同已证明UI可达。
- 生成我方战斗角色真实函数外层挑战争我方阵位点.阵位号==save[角色,5,1]，生成核心的阵位号/数据阵位号都读新save；外层未见新位置是否已有核心的排他条件。下阵后save为0意味着无对应阵位点；已有核心重叠及旧网格/zdls清理仍须连同特殊技能机制与刷新调用核实。业务currentPartyUnit仍旧位置，明确未修复，不能宣称对齐。
- 可见APT_DESC.定力去掉旧内防×2.5、气血×4及控制抗性1.2%/点；按当前stats.coreStat的resolve来源改为每点内防增幅1%、气血增幅0.5%，同类增幅相加。仅改这条与控制技能误导直接相关的说明，其他旧五维说明尚未全量审计；未做浏览器悬浮提示验收。
- 本轮完整49文件514测试、构建通过（既有chunk提示）；之后仅修改定力说明文案。376条数据核验0差异、逐ID报告重建仍376pending。临时_tmp-formation-audit.mjs已删除，所有变更未提交/推送，13张用户截图保留。
- 剩余：复活编队位置与261运行验收、原客户端逐帧/step碰撞及family间接几何、最终逐ID验收；定力数值已改但页面展示仍待必要验收。目标继续active。

## 第三十一轮进展

- 上轮为有效修复。本轮追踪原版编组按钮生成条件，已修复当前正常页面战斗期间可以改阵的问题，不盲目新增原版异常重叠核心模型。
- 原版刷新顶部按钮先销毁旧主要按钮；用途非穿越时，其子分支0为OR（原节点[2]===true）匹配战斗/狩猎/训练场/秘境/副本/幻塔/兽潮/兽王/镇兽/异界/天梯，只创建角色按钮；分支1为Else，才创建编组等按钮。之前只查打开编组handler无锁不够，UI并非无条件可达。sources新增formationButtonGeneration保存OR、用途条件、主要按钮动作和Else创建编组动作，生成器断言维持该分支结构。
- 当前保留阵容查看，但formationLocked统一检查session.combat或pendingCombatRestart。禁用下阵/自动列阵/悉数下阵按钮、关闭名单和阵位draggable；点击名单仍显示详情却不进入布阵选择，点击空格不改位置。main对拖拽start/over/drop以及强制点击修改handler均设防；开始战斗后的旧点选通过viewModel清空。退出战斗后正常编辑恢复。debug placeHero仍可作为测试注入，不据此宣称正常UI可改阵。
- 延续game-playtest技能，真实Edge运行formation.spec.ts全部6项通过：原桌面交换/下阵、触屏点放/下阵、布局，以及新战斗锁定回归。新回归涵盖点击空格、伪造拖拽入槽/拖到名册、移除DOM disabled后强制点击三个修改动作；持久formation始终不变，退出后郭靖可正常上阵。实际查看formation-battle-locked.png，锁定提示、禁用按钮和详情正常，无新增溢出/重叠。
- 复活完整分支证据：先按旧目标zdls[0]读角色，刷新角色战斗属性写到save[角色,5,1]新阵位，再重新读旧目标zdls[0]调用生成我方战斗角色，之后销毁旧目标死亡形象。未清旧网格角色编号。若外部直接改存档到已占阵位/0，可能出现原版共享数组或无核心情况，尚不作为已验证行为。正常页面改阵已按原版禁用，currentPartyUnit临时旧位置不再有已知正常页面差异，但外部状态注入边界保留。
- 完整49文件514测试、构建通过（既有chunk提示），81runtime证据生成通过。之后只更新证据文档；临时_tmp-revival.mjs将精确删除。13张用户截图保留，所有变更未提交/推送。
- 下一步：261自动技能回能/特殊分支运行回归、原版live逐帧碰撞和family几何、逐ID最终验收。当前coverage逐376仍pending，不把UI锁定等同全技能完成。

## 第三十二轮进展

- 上轮为有效UI对齐。本轮完成261自动组合行为回归、排除family间接几何路径，并真正启动原版资源包建立Worker审计入口。
- 新engine回归落云回天阵261：施法者4能量、队友4、阵亡队友0，立即消耗4后全队存活者+2，结果2/5/0；阵亡者不在回能目标，技能核心自身仍活使特殊复活条件不成立，无复活/治疗事件；1200ms后写16000ms冷却且不重复回能。全部49文件515测试通过。实际npm test命令即使传特定文件仍因package.json带src跑全量，输出以实际515为准。
- runtime新增战斗获得能量（现82节点），grant-energy逐ID证据关联该函数。核对其Math.round(clamp(能量+获得量,0,5))表达式。当前普通技能表使用整数回能，但所有能量属性/外部小数状态是否需统一round尚未最终收口，不从本测试推导所有数值边界。
- 原C3加载器明确for familyData of projectData[4]，首项为family类型，其余为成员；_LoadFamily注册正反成员关系。全3family：数字输入15、全文本292、全数量文本4；均不包含核心245/弹体255，数量也等于project[3]family=true计数。sources保存objectFamilies/combatGeometryFamilies及两段原loader代码，断言核心/弹体没有族。family间接几何疑点已排除；逐帧/step碰撞仍未测量。
- 创建可复用审计工具scripts/serve-original-package.py，直接用zipfile只读服务相邻诸天刷宝录/.nw的完整364资源，localhost4175，无全量解压、不写原目录。scripts/probe-original-runtime.mjs用新Edge context、阻断外网、独立临时浏览器存储，不接触用户NW.js存档。
- 未改包直接启动失败require is not defined。仅Playwright拦截scripts/main.js将exportType nwjs改html5后成功进入原版启动屏/主菜单（按Enter），页面无异常；实际查看test-results/original-boot.png。之后追加C3_InitRuntime包装器，仅暴露runtime引用，不改战斗代码；从workermain.js真实Worker拿到self.__auditRuntime，其他dispatchworker/jobworker无引用。
- 初次尝试DOM接口_GetLocalRuntime因真实Worker模式抛not available in worker mode，已改Worker评估，无需关闭Worker或改模拟时钟。下一轮通过page.workers().find(url endsWith workermain.js).evaluate访问__auditRuntime的GetObjectClassByName、GetMainRunningLayout、GetGameTime、GetTickCount、_InvokeFunctionFromJS等，可开始测实际原版碰撞。
- 原包data.json/c3runtime.js/main.js三个SHA256均与_analysis逐字节一致，保存original-runtime-probe.json，版本证据未漂移。宿主HTML5适配与NW.js原客户端差异须始终注明，当前仅主菜单启动验证，collisionParityVerified=false，不能称战斗实机已验收。
- 本轮服务器session13829已Ctrl-C退出（工具exit1为中断退出），浏览器均finally关闭，没有仍活服务。恢复命令：从egg-jianghu运行python scripts/serve-original-package.py，再node scripts/probe-original-runtime.mjs。临时_tmp-geometry.mjs已删除；两原版探针脚本改为上述正式审计工具名保留。
- 376技能数据0差异、逐ID报告重建；目标仍未完成，未提交/推送，13用户截图保留。下一步原版Worker实战/碰撞测量与逐ID最终验收，而非重复已完成的family静态扫描。

## 第三十三轮进展

- 恢复后补齐上次已运行但尚未记录的原版碰撞探针；本轮正式脚本带完整metadata和断言实际执行通过。
- 6组原版阵位组合各双向共12例，连续projectileFlightMs预测均处在最后未命中帧和弹体消失帧之间。远程预测617.5/77.5/837.5/474.7869ms，原版观测约633.3/83.3/850/483.3ms，差异处于采样帧边界内；保留全部逐帧坐标。不宣称NW.js与HTML5时钟相同或全376技能已验收。
- 隔离fixture必须在各例间等待1100ms清除旧Wait位移，并同步移动核心及其原版数据阵位点；子弹消除会在原目标阵位点碰撞时销毁弹体。旧只移动核心的异常不能用于修正业务。
- runtime现83节点，新增子弹消除、combatContainers；255与256容器关联已记录。原版最终截图实际查看后仍为新手剧情覆盖，虽然Worker已创建16个战斗核心，不能称可见战斗页面验收完成。
- 更新original-runtime-probe.json、coverage和逐ID报告的陈旧碰撞/战斗改阵pending；远程完整300ms施法链与销毁边界仍pending。临时_tmp-tween-audit.mjs已精确删除。

## 第三十四轮进展

- 修复通用耗能、技能回能漏取整：原版失去能量值与战斗获得能量均Math.round(clamp(...,0,5))。spendSkill和executeGrantEnergy已按该公式实现，保留原器魂免耗分支。本轮未修改自然行动回能，其原版表达式仍待独立最终复核。
- 原版Worker直接调用敌方核心两个真实函数，初始0/2.49/2.5/4.49/4.5/7共12例全部通过，保存在original-energy-probe.json；4.5扣4为1，2.5加2为5。业务增加261完整施法小数边界两例，通过实际耗能后回能顺序验证，不只有公式镜像断言。
- 新增原版持续弹体探针original-periodic-probe.json：生命恢复、恢复效果、流血、燃烧、中毒各调用一次，初始5000、效果101；下一观测tick分别5101/5101/4899/4899/4899，弹体已清除。调用时取GetInstances尚未看到zidan1，下一tick已命中销毁，不能称直接观测到完整弹体寿命；这验证真实函数结果和下一tick结算，不代表1000ms调度入口的事件顺序已对齐，也不应硬编码16.7ms。
- 正式探针全部断言执行通过（12碰撞、12能量、5持续效果），页面无异常，浏览器finally关闭。完整49文件517测试通过、构建通过（既有chunk提示）、diff-check通过。逐ID报告仍376pending，未虚标最终验收。
- 下一步：自然行动回能、完整技能300ms生成与持续效果1000ms事件顺序、来源/目标销毁边界、逐ID最终运行覆盖；不用重复已完成的数据/family扫描。所有修复仍未提交/推送；13张用户heroes截图保留，目标仍active。
- 本轮结束已Ctrl-C停止原版只读HTTP服务器session90198（exit1为中断退出）；无仍活浏览器。下次需要live探针时再启动4175。

## 第三十五轮进展

- 上轮为有效修复与live证据。本轮恢复核对Git和checkpoint，继续共享能量/冷却链，未改目标范围。
- 新增原版获得能量值运行证据（现84节点），证实自然行动回能也是Math.round(clamp(当前+回复,0,5))。prepareAction原先不取整且先将负回复置0，现按原函数先求和、clamp、round，再AI选技。新回归：3能量回复0.49仍普攻，回复0.5可释放耗能4的261并回能至2；不能等耗能时再round补救选技门槛。
- 原版角色行动Wait1.2后的CD分支检查器魂生效123，Else直接写0；旧只有最低5%缩减不等价。completeActionCooldown新增我方副手器魂123直接清零。原版召唤zdls[0]保留主人角色编号，因此此处按summonerId查主人器魂；免耗129仍有自身非召唤限制，不混用。更新CombatUnit字段注释。
- 增加普通角色有无器魂123的1199/1200ms冷却边界回归，以及主人存活/阵亡时召唤读取主人效果回归。召唤测试注入261验证读取机制，不声称原版9模板实际携带该技能。
- 原版Worker探针增加自然回能，现18组耗能/技能回能/自然回能结果全部通过。新增original-cooldown-probe.json：在隔离原版新档用真实wp1691副手器魂123、真实角色1调用角色行动(阵位,261,1,true)，100%冷却属性下无器魂为0.8秒、有器魂为0，均在调用后约1400ms读取。保留原战斗代码，仅设置隔离数组和选中技能全局变量；未修改用户存档。
- 探针session76288实际exit0：12碰撞、18能量、5持续效果、2完整行动冷却全部通过。此后仅追加脚本最终errors检查（追加行尚未再次执行；旧pageerrors检查此前已通过）。浏览器finally关闭。
- 94项engine专项、完整49文件523测试、构建通过；376技能独立数据0差异，逐ID报告重建仍376pending，diff-check通过。所有修改未提交/推送，13张用户截图保留。
- 新明确待核：原版CD器魂分支没有像免耗分支一样检查阵营，敌方zdls[0]也写敌人角色编号，可能读取同编号我方save副手。当前只对我方及其召唤处理123；应原版live验证该跨编号行为，再查当前自定义英雄/原版角色编号映射，不能直接假定敌方不受影响或盲目按名称映射。
- 后续仍需完整300ms弹体生成链/来源目标销毁、1000ms持续效果实际事件顺序、逐ID最终运行覆盖。不要将已通过代表案例等同376技能全部验收。
- 本轮已Ctrl-C停止HTTP原包服务器session96913（exit1为主动中断），没有待轮询的原版进程。

## 第三十六轮进展

- 上轮为有效修复。本轮核验敌方同编号器魂并补上业务，保留全技能目标，不提交/推送。
- 原版探针最初找不到与主角1相同数据编号的敌人：当前新手剧情下生成核心存活但zdls[0,22,0]为0。改为显式向敌方数据槽写1构造隔离条件，报告保留previousEnemyRole0/injectedEnemyRole1；不能把该fixture当自然生成实战。
- 初次四案例出现未装备也CD0。追踪发现结束战斗行动Wait0.5后清空选中技能编号/槽编号；旧每例1400ms，上一例1700ms清理覆盖下一例选技。等待改1900ms，并暂停自动积攒隔离案例。失败探针都已终止，不解释为原版敌人天生无冷却，也不修改业务迁就失败。
- 新增原版结束战斗行动、结束战斗操作证据（86节点）。真实函数结束战斗行动先角色行动false，另Wait0.5后战斗暂停false、选中技能及当前操作角色等清0；结束战斗操作仅恢复模型大小并调用关闭战斗操作菜单。
- 最终--cooldown-only探针session59288 exit0，普通我方/同编号敌方×器魂123无/有四例全通过，原版CD分别0.8/0/0.8/0秒，完整最终pageerrors断言已执行通过。original-cooldown-probe.json记录实际数组槽与选择状态。新增--cooldown-only避免反复跑无变化的碰撞/持续观测。
- 原版刷新角色战斗属性敌方分支确实zdls[X轴=0,阵位,0]=角色编号；CD器魂判断没有阵营过滤。当前HeroDefinitionV10.sourceId已明确定义原版角色映射：主角1、原版势力角色有编号，自创侠客不猜配。
- CombatStartInput/CombatSnapshot新增cooldownBypassRoleIds快照，由GameSession扫描全部已招募角色的原版sourceId及实际副手器魂123生成，不只上阵角色。createEnemy记录sourceRoleId=drId；completeActionCooldown对敌人按编号列表清零。换波重建敌人仍保留drId和状态列表。无对应原版编号的自创英雄不加入列表。
- 回归：主角未上阵但装备123仍输出[1]，卸下清空；引擎进入第二波后按实际drId匹配/不匹配时三连击CD0/12000，出战队友未持该器魂。两文件104专项测试，完整49文件526测试、构建和diff-check通过。逐ID报告重建仍376pending。
- 下一步高优先级：结束战斗行动的额外500ms是否让正常自动积攒及下一次行动也等待（战斗行动积攒外层确有!战斗暂停）；继续追关闭战斗操作菜单是否提前解除暂停及完整原版事件调用，不能直接加500ms或忽略。还剩300ms弹体完整生成链、来源/目标销毁、1000ms持续事件顺序、逐ID最终验收。
- 修复与证据仍未提交/推送，13张用户heroes截图保留。当前没有跑中的浏览器或构建；HTTP服务器session78610将在本轮收尾停止。
- HTTP服务器session78610已Ctrl-C退出（exit1为主动中断），无待轮询原版进程。

## 第三十七轮进展

- 上轮为有效修复。本轮核对结束战斗行动后的暂停路径并修复，未提交/推送。
- 关闭战斗操作菜单只销毁UI/关闭分类和tips，没有提前解除暂停；全project战斗暂停写入只有积攒时设true、结束战斗行动Wait0.5后false，以及换波/战斗结束等独立流程false。原版战斗行动积攒和持续效果Every1的共同外层都要求!战斗暂停。
- 新增关闭战斗操作菜单与战斗技能及buff冷却、生命恢复、燃烧、中毒等两个runtime节点，现88。sources新增battlePauseWrites保存全部动作，以及Every条件真实实现everyConditionSource。
- 原版--cooldown-only新增暂停状态采样，四案例均先acting1/paused1，约1200至1216.5ms为acting0/paused1，约1700至1733ms为acting0/paused0。首轮断言因1199.9999999999993浮点误差失败，仅加入1e-6ms误差容限；正式重跑session22507 exit0，四冷却与四暂停案例及最终pageerrors全通过。不是把实体时差放宽到容忍错误。
- 引擎新增timeline.resumeAtMs：1200ms写CD、解除activeAction、判死后，将积攒恢复时间设为当前+500。startNextAction在该时点前不出队；advanceCombat暂停段只前进模拟时间，不推进属性/CD/状态。换波与结局原有流程继续并行前进绝对时间，因此不会额外重复500ms。死亡、收益和换波起点仍1200ms。
- 原526测试两条旧预期失败，已依据原版改正：队首下一动作需收尾后再等499/1ms边界；持续状态在新增500ms内不减时。完整49文件526测试和构建通过，diff-check通过。没有把这两条修正视为持续脉冲全语义完成。
- **新发现待修：Every(1)恢复后相位。** 原C3 System.Cnds.Every保存Every_lastTime，直接比较runtime.GetGameTime；达到阈值先lastTime+=seconds，若落后当前>=0.04秒则重置lastTime=curTime。父条件暂停只阻止该条件求值，不冻结其全局时钟。因此当前statusPulseCarryMs仅随积攒推进可能错误：长行动结束后原版可能立即跳一次，不应再等冻结的剩余900ms，也不应一次追补所有漏跳。当前关于冻结carry的测试只能证明当前代码，已明确不能当原版等价证据。
- 下轮优先：原版Every恢复后的单次触发/相位与事件顺序实测，再改持续效果调度；需要保留冷却/状态持续时间按积攒递减的独立语义，不能把两者一起按墙钟推进。之后完整300ms发射与来源/目标销毁、逐ID最终验收。
- 逐ID仍376pending。所有改动未提交/推送，13用户heroes截图保留。临时_tmp-action-pause.mjs将精确删除，HTTP服务器session60477将收尾停止。
- 原包服务器session60477已Ctrl-C退出（exit1为中断），浏览器已关闭，临时脚本已删除，无待轮询进程。

## 第三十八轮进展

- 上轮为有效暂停修复。本轮修复Every全局时钟语义，没有启动浏览器/原包服务，无待轮询进程。
- consumeStatusPulse放在现有scheduler：到lastTime+1000时只返回一次true；先lastTime+=1000，若当前>=lastTime+40则重置lastTime=当前；时钟回退超过100ms时重置。由原System.Cnds.Every直接翻译，不使用while追补。timeline.statusPulseCarryMs已替换为lastStatusPulseAtMs。
- tickRealtime仍只在原有积攒入口调用持续效果判定，但以state.elapsedMs求值；暂停期间不调用，恢复后到期只跳一次，下一周期由新相位决定。状态剩余时间/CD递减仍按积攒步推进，未改为全局时钟。
- 新增scripts/check-original-periodic-clock.mjs直接读原c3runtime.js，截取未改写Every方法并执行，使用相同输入分别比较当前helper。6组31采样覆盖正常秒跳、暂停恢复、多秒遗漏、39ms迟到保相位、40ms迟到重置、时钟回退；全部原函数布尔结果及lastTime一致。original-periodic-clock-audit.json保存源SHA256、真实方法源码和全部采样。此为原函数重放，不是浏览器完整事件调度验收。
- engine原冻结carry测试预期按新证据纠正：100ms起手后1200ms动作+500ms暂停，下一100ms积攒在1900ms只跳一次，900ms内不重复，2900ms再跳；暂停内状态时长保持。主人阵亡fixture的指定脉冲相位改为lastPulse=当前-900。
- 完整49文件526测试和构建通过。diff-check最初发现脚本写入测试文件多余EOF空行，已修正并重新通过；之后仅改测试局部变量名frozenCarry为frozenPulseTime，无业务修改。
- 剩余明确：当前原版Everyhelper已对齐，但tickRealtime在100ms积攒入口且先于AI出队调用；原版Every位于独立事件组，需要核对组顺序、每帧求值及连续满队列行动期间是否错过/恢复脉冲。这些不能由31函数采样或当前绿测证明。后续优先原版事件顺序/完整运行观测，再300ms发射与销毁、逐ID最终覆盖。
- 目标仍active，376逐ID最终验收未完成；所有更改未提交/推送，13用户heroes截图保留。

## 第三十九轮进展

- 上轮为有效Every修复。本轮确认原版事件顺序并修复持续效果抢在选技前结算的差异，没有启动浏览器或原包服务器。
- 原始project[6][5][1]中战斗行动积攒为索引40，持续效果组为41。原C3.EventSheet constructor按data[1]创建_events，Run按for(const e of this._events)e.Run(frame)依序执行。sources新增battleEventGroupOrder，记录表名、两索引和Run源码；生成器断言两组相邻且执行循环仍存在。
- 积攒组启动行动即设置战斗暂停true；其后的Every组要求!战斗暂停，因此本帧起手后不能先跳持续效果。引擎将advanceBattleTimers与pulseRealtime分开：先计时/入队/启动行动，仅仍在accumulating且无activeAction时求值Every并结算持续效果。删去空返回事件的旧tickRealtime接口。
- 召唤时间递减现在位于计时阶段（原版积攒组确有核心[22]-=dt），此前在pulse之后；召唤到期销毁的实际组位置仍需连同核心生命周期最终检查，不能只凭原时长回归作为全行为证明。
- 新回归：时间900、满行动条、HP5且致死DoT10；1000ms先skill-used且不触发DoT，lastPulse仍0，1700ms行动+暂停结束后存活，下一积攒节点2800ms才DoT致死。旧顺序会在1000ms直接死亡且不出手。
- 88runtime证据生成通过，完整49文件527测试、构建通过，diff-check通过。临时_tmp-event-order.mjs将删除；13张用户heroes截图保留，所有改动未提交/推送。
- **新增关键待修：原版积攒并非固定100ms。** 战斗行动积攒核心AddInstanceVar是dt()*sqrt(zdls7*clamp(1+zdls113/100))*2，没有Every0.1条件。其中Wait0.1位于我方自动出手前（包括召唤自动分支），敌方出手分支没有该Wait。当前scheduler的“原版两处分支均以0.1秒推进节点”注释及全局100ms离散积攒把起手延迟与dt积分混淆。下一轮需修正积攒/选技前延迟的独立机制，并核对冷却倒计时函数里的dt、每帧Every、队列满行动条重置时点；不要只改常量或把敌我都加100ms。
- 后续仍需每帧原版完整行为对照、300ms弹体生成及来源目标销毁、召唤到期顺序、逐ID最终验收。目标仍active，376技能最终验收未完成。

## 第四十轮进展

- 上轮为有效事件组顺序修复。本轮继续积分公式与状态到期顺序，有业务修复，未做浏览器运行，没有待轮询进程。
- 原版核心增长表达式dt()*sqrt(zdls7*clamp(1+zdls113/100,0,Infinity))*2，冷却及状态时间函数直接减dt；确认不是固定100ms的原版条件。scheduler的误导注释已改为“当前模拟步长”，没有假称全局离散调度已经修好。
- timeline.advanceGaugeAndCooldowns去掉通过四舍五入行动间隔积分的误差，直接elapsedMs*sqrt(speed)/50（原版100行动条映射本作1000）。actionIntervalMs移除round与速度最小1截断，0速度为Infinity。effectiveSpeed使用unitAttr(SX.速度修正)，包含基础属性与buff，修复旧只读buff导致基础修正遗漏。
- timeline测试直接导入原版runtime证据的实际AddInstanceVar表达式，用Function执行原表达式，6种速度(0/0.25/1/2/100/137.2)×5修正(-150/-100/-50/0/50)，1234.5ms积分30组全部一致。首版测试import node:fs虽vitest通过但浏览器tsconfig不含Node类型导致构建失败；已改Vite ?raw导入证据，不加依赖/类型配置。专项和构建重新通过。
- advanceBattleTimers原先先移除到期状态再算该步速度，原版事件组先所有核心增长，再全场冷却/状态倒计时。现拆为先全场advanceGaugeAndCooldowns，再按原阵位顺序移除状态并刷新召唤属性。新回归控制剩100ms：该步增长0、状态消失，下一步恢复增长。
- 完整49文件529测试通过、构建通过（既有chunk提示）、diff-check通过。13张用户heroes截图保留，所有修改未提交/推送。
- **下一步不能回避的主项仍是调度改造**：当前100ms全局步长未移除，不能用公式30组一致证明每帧行为一致。原版仅我方自动起手Wait0.1，敌方立即调用开始战斗行动；需独立pending-action/起手阶段，并以原版dt/事件边界处理增长、状态、Every与队列。当前我方等待碰巧被全局100ms步长遮盖，不应简单两边都加100ms。后续还需满条时原版行动条归0/权重分配、实际原版连续行动观测及每技能最终覆盖。
- 376逐ID最终验收仍未完成，目标active；本轮无临时脚本、无运行服务。

## 第四十一轮进展

- 上轮为有效积分公式修复。本轮完成主要调度拆分，不再把我方Wait0.1当全局积攒周期。
- timeline新增preparing/pendingAction，只有我方自动出队后等待100ms，再执行原有回合状态到期、自然回能、AI选技、耗能及技能释放；敌方立即prepareAction。准备期间冻结积攒/状态计时/脉冲，stop清理pendingAction。
- 移除accumulationCarryMs和全局固定100ms积攒；nextAccumulationBoundary按最近满行动条、前12槽time状态到期、召唤时长、Every时点或本次剩余时间拆段。每段直接按dt积分，边界先入队/起手，再检查可达的持续效果。COMBAT_TICK_MS100仅保留tick(count)调用单位，不作为内部物理步长。常量更名ORIGINAL_PARTY_PREPARATION_MS。
- 入队时行动条归0，对已有readyQueue成员冻结行动条增长，保留已有readySeq；满条比较仅加入1e-8数值容限处理连续积分尾差。原版同一帧超额行动条排序/跨阵营实例顺序还需实机最终核对，不能把连续数学事件顺序直接当每帧完全一致。
- 新回归：我方99ms没有skill-used且不回能，100ms选技并回能；敌方已满条advance1时在0ms启动。速度10000、初始条900，49ms条998，50ms满条进入preparing且归0，150ms才发出skill-used。原来的两条脉冲测试去除100ms积攒延迟，恢复后时点2700/1800。
- 两分钟单次补算和100ms逐步推进产生唯一差异为某敌行动条666.3692669984114/666.3692669984117，收益/物品等完全相同。该测试仅行动条使用10位小数容差，其余快照字段仍逐项deep equal；最初补丁误落到相邻跨结算测试，已移动到正确两分钟测试，并恢复相邻测试原严格断言。
- 新phase首次构建查出IdleCombatModel未更新，已补preparing类型；main用pendingAction.actorId作为准备阶段行动者，UI显示准备出手。文字在原有区域，不引入布局结构；本轮没有浏览器视觉验收。
- 完整49文件531测试通过；之后只改UI准备状态文字和行动者显示映射，最终构建通过（既有chunk提示），diff-check通过。13用户heroes截图保留，全部未提交/推送，本轮无运行浏览器/服务/临时脚本。
- 下一步：实际原版自动出手链（尤其我方100ms与敌方0ms）及连续积攒的观测对照、同帧满条排序/角色实例顺序、每帧状态到期与Every执行顺序、召唤队列中时长条件；随后300ms弹体发射及来源/目标销毁和逐ID最终验收。当前连续事件模型不宣称与不同宿主原版帧时间完全相同。
- 目标仍active；376逐ID最终运行验收未完成，不能仅凭531测试收口。

## 梵行无终串扰修复（2026-09-10）

- 按用户要求移除跨阵营编号关联；未上阵英雄的副手不再进入全局冷却判断。
- 更新回归：我方装备或卸下器魂，后续波次敌人的三连击均保持 12000ms 冷却；未上阵装备不改变战斗输入；123 副手装卸、我方零冷却和主人存活/阵亡时召唤继承回归通过。
- 本次 4 文件 155 项相关测试通过，npm run build 通过（既有大 chunk 提示）。未提交、未推送，未触碰用户截图。

## 整体复查（2026-09-10，梵行无终修复后）

- 范围：敌人技能配置、技能数据、选技门槛与回退、伤害/治疗/护盾、状态结算、召唤/复活及阵营装备关联；复用本地原版解码证据，不追逐帧等价。
- 当前验证：完整 49 文件 532 测试通过；重新执行 check-original-skills.mjs，376 技能、260 敌人配置、67 状态、9 召唤模板、36 范围及所核验字段差异为 0。数据通过不代表全部技能运行无缺陷。
- 新发现：statuses.ts 允许状态分配到第 13 槽以后，但 advanceStatusDurations 与 pulseStatuses 只处理前 12 槽。现有 timeline.test.ts 还将这种旧原版对齐行为作为正确预期。
- 通过 Vite SSR 加载实际模块复现：用 applyBuff 依次施加 12 种真实状态后施加眩晕（ID 2），自动分配 slot 13；advanceStatusDurations(60000) 后 remainingMs 仍为 3000，速度修正为 -100。独立案例把中毒（ID 3、tickValue 50）置于第 13 槽，pulseStatuses 返回空且 HP 不变。未手工赋值槽号；这是构造多状态组合的领域复现，未验证正常配队中的触发频率。
- 建议按本作规则修复：所有已接纳的时间型状态均正常倒计时，所有持续效果均正常结算；同步移除引擎事件边界的前 12 槽限制，修改旧测试预期。不要继续保留原版槽位实现缺陷。
- 本次仅检查并更新进展，未修改业务代码；除该项外，复查范围内未确认其他必须修改的重大问题。没有宣称全部 376 技能逐一实战验收，无浏览器进程遗留，无提交或推送。

## 提交收口（2026-09-10）

- 移除状态倒计时、持续效果脉冲及引擎下一事件边界中的前 12 槽限制；保留槽位顺序和回合状态规则。
- 更新旧限制回归，新增通过 applyBuff 自动分配第 13 槽眩晕的引擎回归：300ms 到期后恢复行动条，大步 600ms 与六次 100ms 推进一致。
- 完整 49 文件 533 测试、构建通过（既有大 chunk 提示）。首次专项误从仓库根运行且发现测试文本替换波及一处无关 HP 断言，已恢复该断言并在 egg-jianghu 目录完整验证。
- 本次按用户要求收口当前 worktree 的战斗修复、页面修复、生成器和核验资料。根目录 heroes-*.png 为本机 UI 调整截图，新增忽略规则，13 张原文件保留；未发现需要删除的未跟踪临时文件，不删除可复用脚本及有效证据。依赖、构建与测试输出已有忽略规则。
- 历史“未提交”“必须继续”仅描述当轮状态，以上述新目标和本次收口为准；不推送或部署。

- 提交前页面验证：Edge 下 formation.spec.ts 与 hero-martials.spec.ts 共 12 项通过；已查看 390px 武学页面截图，未见明显重叠。git diff --check 通过。
