# 项目协作约定

适用于本仓库及所有子目录。

## 修改完成后自动提交

- 每轮涉及代码、测试、配置或文档的修改，完成本轮任务并通过直接相关验证后，必须执行 Git 提交，不需要再次询问用户是否提交。
- 按一个完整、可验证的修改批次提交，不要求每次编辑文件后立即提交。只读调查不创建空提交；用户明确要求暂不提交时遵从用户指令。
- 提交前检查 `git status` 和 diff，保留用户已有及无关改动。日常只提交本轮相关文件；用户要求「提交现有内容」时收口当前 worktree 的全部有效改动。
- 不提交密钥、本机配置、缓存、runtime 文件、依赖或构建产物。验证失败或存在真实阻塞时先解决；无法解决时如实说明，不将未完成工作冒充已验证提交。
- 提交说明使用 `<emoji> <type>(<scope>): <中文说明>`，scope 可省略；说明不以句号结尾，单行不超过 150 字符。
- 完成后报告 commit、验证结果及必要的排除项。自动提交仅授权本地 commit，推送和部署遵循用户另外提供的授权。

## 项目验证

- 应用目录为 `egg-jianghu`，在该目录运行相关单测、构建和 Playwright 测试。
- 界面修改检查桌面与手机显示；存档修改核对旧档兼容性和数据保留。
- 所有工作由当前 Agent 直接完成，不调用子 Agent、`claude-worker` 或 `codex-errand-worker`。
